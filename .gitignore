# Compiler output
cache/
out/

# Broadcast logs (keep production, ignore local/dry-run)
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Node.js
node_modules/

# Environment
.env


# Aderyn report
report.md

# Cursor (commit .cursorignore so clones skip out/ and lib/ in search).
# Keep .cursor/rules/ versioned; ignore everything else (e.g. relaunch-plan.md).
.cursor/*
!.cursor/rules/
!.cursor/rules/**