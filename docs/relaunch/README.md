# Relaunch task specs

Committed work orders for BitChill contract PRs. Implement from a spec here. The human prompt is `Start with R<n>` (see `AGENTS.md`).

## Progressive disclosure

1. Root [`AGENTS.md`](../../AGENTS.md).
2. [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md) — PR map and which product gates to ask.
3. **One** assigned spec in this folder (write it from [`TASK_TEMPLATE.md`](./TASK_TEMPLATE.md) if missing).
4. The files it names, then only imports / inheritance / mocks / failing tests / compiler errors.

Document any extra files in the PR. Do not search `out/`, `cache/`, or `lib/`. Do not implement from gitignored `.cursor/relaunch-plan.md` (read it only to draft the missing spec).

## How to add a spec

1. Copy [`TASK_TEMPLATE.md`](./TASK_TEMPLATE.md) to `R<n>-<short-slug>.md`.
2. Fill **Open product decisions** (or write **none**). Ask the human only if that section is not none and not yet answered.
3. Every remaining relaunch item is ordered in `IMPLEMENTATION_ORDER.md`; there is no optional-late queue.
4. One spec = one PR unless **Scope** names a tight bundle.

## Decision records

- [`EXTERNAL_REWARDS.md`](./EXTERNAL_REWARDS.md) (GitHub [#57](https://github.com/BitChillRSK/dca-contracts/pull/57)) — lending handlers distribute native protocol interest only; external campaigns use off-chain forwarding if available. R9 must add a canonical per-user lending-share balance-transition event so forwarding does not depend on transaction traces or a provider response.

## Measurement basis

Every runtime size, EIP-170 margin, and gas figure in this folder is measured under `[profile.default]`:
solc 0.8.36 / `cancun`, **`optimizer = true`, `optimizer_runs = 200`, `via_ir = false`**. That pin landed
in R52 / [#104](https://github.com/BitChillRSK/dca-contracts/pull/104). Before it, `[profile.default]`
never set `optimizer` and forge defaults it to `false`, so **every figure recorded for a PR that shipped
before #104 is unoptimized**: those numbers stay in place as the basis their decision was actually made
against, and are not today's budget. R53 re-baselines the record and changes no code, test, or build
setting.

Fresh full `forge build --sizes` at this branch's head (2026-09-02, R58 stack), EIP-170 24,576 B:

| Contract | runtime (B) | margin (B) |
|---|---:|---:|
| `DcaManager` | 13,767 | 10,809 |
| `OperationsAdmin` | 3,227 | 21,349 |
| `IdleDocHandlerMoc` | 7,539 | 17,037 |
| `SovrynDocHandlerMoc` | 10,724 | 13,852 |
| `LayerBankDocHandlerMoc` | 10,933 | 13,643 |
| `TropykusDocHandlerMoc` | 10,864 | 13,712 |
| `SovrynErc20HandlerDex` | 15,479 | 9,097 |
| `LayerBankErc20HandlerDex` | 15,692 | 8,884 |
| `TropykusErc20HandlerDex` | 15,623 | 8,953 |

Gas on the MoC/Sovryn lane (`SWAP_TYPE=mocSwaps LENDING_PROTOCOL=sovryn STABLECOIN_TYPE=DOC`):
`testSinglePurchase` **243,790**, `testBatchPurchasesOneUser` **1,562,441**.

Two things to keep straight when reading a historical entry against this table. These are today's head,
not a rebuild of each past PR's commit, so the delta against an old entry mixes the optimizer flip with
every source change since. For the same-commit scale of the flip alone, #104 measured `DcaManager`
23,703 → 13,767 B (margin 873 → 10,809), `testSinglePurchase` 283,711 → 243,817 (−14.1%), and
`testBatchPurchasesOneUser` 2,244,557 → 1,562,720 (−30.4%) — the optimizer alone reclaims ~9.9 KB of
runtime and 14–30% of purchase gas. The three Dex leaves are 100 B above the figures #104 recorded
because R56 / [#107](https://github.com/BitChillRSK/dca-contracts/pull/107) landed since; `DcaManager`
and `OperationsAdmin` are unchanged from #104.

Several shipped decisions were argued from bytecode scarcity that was an artifact of the unoptimized
measurement. R53 listed them; [#111](https://github.com/BitChillRSK/dca-contracts/pull/111) closed the
queue in
[R53-optimizer-baseline.md](./R53-optimizer-baseline.md#bytecode-scarcity-decisions-closed)
(R31 / R39 / R42 / R51 shipped and stand; R13 cooperative migration and R50's merged per-user slot stay
decided against). No further re-judgement PRs.

## Status

- Merged: [R23-toolchain-baseline.md](./R23-toolchain-baseline.md) (PR 1, GitHub [#42](https://github.com/BitChillRSK/dca-contracts/pull/42)). Rootstock testnet accepted solc 0.8.36 / `cancun`.
- Assigned: [R2-utc-purchase-period.md](./R2-utc-purchase-period.md) (PR 3, GitHub [#44](https://github.com/BitChillRSK/dca-contracts/pull/44)). No product gates. Does not wait on PR 2.
- Assigned: [R7-dca-manager-small-fixes.md](./R7-dca-manager-small-fixes.md) (PR 4, GitHub [#45](https://github.com/BitChillRSK/dca-contracts/pull/45)). Bundles R7 / R11 / R14. No product gates. Stacked on [#44](https://github.com/BitChillRSK/dca-contracts/pull/44).
- Assigned: [R3-fee-handling.md](./R3-fee-handling.md) (PR 5, GitHub [#46](https://github.com/BitChillRSK/dca-contracts/pull/46)). Bundles R3 / R4 / R5. Keep linear (gate answered this chat). Stacked on [#45](https://github.com/BitChillRSK/dca-contracts/pull/45).
- Assigned: [R6-hot-path-cleanup.md](./R6-hot-path-cleanup.md) (PR 6, GitHub [#47](https://github.com/BitChillRSK/dca-contracts/pull/47)). Bundles R6 / R17. Spec revised after review and re-implemented. No product gates. Stacked on [#46](https://github.com/BitChillRSK/dca-contracts/pull/46).
- Assigned: [R8-remove-stuck-rbtc-rescue.md](./R8-remove-stuck-rbtc-rescue.md) (PR 7, GitHub [#48](https://github.com/BitChillRSK/dca-contracts/pull/48)). No product gates. Stacked on [#47](https://github.com/BitChillRSK/dca-contracts/pull/47).
- Assigned: [R1-integration-cash-accounting.md](./R1-integration-cash-accounting.md) (PR 8, GitHub [#49](https://github.com/BitChillRSK/dca-contracts/pull/49)). Bundles R1 / R20 and owns the R6 clamp-desync leftover. No product gates. Stacked on [#48](https://github.com/BitChillRSK/dca-contracts/pull/48).
- Assigned: [R24-test-harness-matrix.md](./R24-test-harness-matrix.md) (PR 9, GitHub [#50](https://github.com/BitChillRSK/dca-contracts/pull/50)). Test/Makefile only. Stacked on [#49](https://github.com/BitChillRSK/dca-contracts/pull/49). Merge after #49.
- Assigned: [R15-withdraw-all-sentinel.md](./R15-withdraw-all-sentinel.md) (PR 10, GitHub [#51](https://github.com/BitChillRSK/dca-contracts/pull/51)). Withdraw-all sentinel only; lending-share dust deferred (decision recorded in the spec). No product gates. Stacked on [#50](https://github.com/BitChillRSK/dca-contracts/pull/50).
- Assigned: [R22-repo-layout.md](./R22-repo-layout.md) (PR 11, GitHub [#52](https://github.com/BitChillRSK/dca-contracts/pull/52)). Folders only; no product gates. Stacked on [#51](https://github.com/BitChillRSK/dca-contracts/pull/51).
- Assigned: [R22-idle-handler.md](./R22-idle-handler.md) (PR 12, GitHub [#53](https://github.com/BitChillRSK/dca-contracts/pull/53)). Idle DOC + MoC handler at index 0. No product gates. Stacked on [#52](https://github.com/BitChillRSK/dca-contracts/pull/52).
- Assigned: [R21-fee-on-transfer-deposits.md](./R21-fee-on-transfer-deposits.md) (PR 13, GitHub [#54](https://github.com/BitChillRSK/dca-contracts/pull/54)). Hop-1 received-on-deposit. FOT unsupported; withdraw still works if a listed token turns on a fee. No product gates. Stacked on [#53](https://github.com/BitChillRSK/dca-contracts/pull/53).
- Assigned: [R16-redeem-glossary.md](./R16-redeem-glossary.md) (PR 14, GitHub [#55](https://github.com/BitChillRSK/dca-contracts/pull/55)). Rename-only; first-party "redeem" now names the asset given up. Keeps PR 13's 1-wei batch-redemption event check. No product gates. Stacked on [#54](https://github.com/BitChillRSK/dca-contracts/pull/54).
- Supporting: [EXTERNAL_REWARDS.md](./EXTERNAL_REWARDS.md) (GitHub [#57](https://github.com/BitChillRSK/dca-contracts/pull/57)). Docs-only external-incentive boundary and indexer-ready R9 share-event requirement. Stacked on the live Sovryn probe [#56](https://github.com/BitChillRSK/dca-contracts/pull/56); no Solidity or ABI change in this PR.
- Assigned: [R22-layerbank-handler.md](./R22-layerbank-handler.md) (PR 15, GitHub [#58](https://github.com/BitChillRSK/dca-contracts/pull/58)). LayerBank DOC + MoC handler at index 1. No product gates. Stacked on [#57](https://github.com/BitChillRSK/dca-contracts/pull/57).
- Assigned: [R25-lending-redeem-naming.md](./R25-lending-redeem-naming.md) (PR 16, GitHub [#59](https://github.com/BitChillRSK/dca-contracts/pull/59)). Rename-only: `_redeemByUnderlying` / `_redeemByShares`, `*ToRedeem` locals, Sovryn interest-local alignment, and the unused `minPurchaseAmount` constructor arg dropped from the Tropykus/Sovryn leaves. No product gates. Stacked on [#58](https://github.com/BitChillRSK/dca-contracts/pull/58).
- Planning: GitHub [#60](https://github.com/BitChillRSK/dca-contracts/pull/60). Specs only: R26, R27, and R28 (later promoted to PR 19). Stacked on [#59](https://github.com/BitChillRSK/dca-contracts/pull/59). Later reordered so R27/R28 precede R22 deploy/CI — see [#62](https://github.com/BitChillRSK/dca-contracts/pull/62).
- Assigned: [R26-share-terminology.md](./R26-share-terminology.md) (PR 17, GitHub [#61](https://github.com/BitChillRSK/dca-contracts/pull/61)). Rename-only: “lending token” → `shares` (`getUserShares`, `_stablecoinToShares` / `_sharesToStablecoin`, `TokenLending__SharesRedeemed(Batch)`, `TokenLending__InsufficientShares`, Sovryn `_redeemShares`). Preserves R16 `underlyingAmount` event params. No product gates. Stacked on [#60](https://github.com/BitChillRSK/dca-contracts/pull/60).
- Assigned: [R27-tropykus-lending-guards.md](./R27-tropykus-lending-guards.md) (PR 18, GitHub [#62](https://github.com/BitChillRSK/dca-contracts/pull/62)). Align Tropykus deposit/batch zero-cash guards with Sovryn/LayerBank. Reordered ahead of R22 deploy/CI. Stacked on [#61](https://github.com/BitChillRSK/dca-contracts/pull/61).
- Assigned: [R28-lending-erc20-handler.md](./R28-lending-erc20-handler.md) (PR 19, GitHub [#63](https://github.com/BitChillRSK/dca-contracts/pull/63)). Extract `LendingErc20Handler` (Idle out). Stacked on [#62](https://github.com/BitChillRSK/dca-contracts/pull/62).
- Assigned: [R29-hardcode-exchange-rate-scale.md](./R29-hardcode-exchange-rate-scale.md) (PR 20, GitHub [#64](https://github.com/BitChillRSK/dca-contracts/pull/64)). Hardcode Sovryn/Tropykus `EXCHANGE_RATE_DECIMALS = 1e18`; LayerBank renamed `RAY` → `EXCHANGE_RATE_DECIMALS` (`1e27`). Stacked on [#63](https://github.com/BitChillRSK/dca-contracts/pull/63).
- Assigned: [R30-purchase-pipeline.md](./R30-purchase-pipeline.md) (PR 21, GitHub [#65](https://github.com/BitChillRSK/dca-contracts/pull/65)). Combines review Candidates A+B: one shared purchase algorithm and one shared stablecoin-source seam; no ABI/product change. Stacked on [#64](https://github.com/BitChillRSK/dca-contracts/pull/64).
- Planning: GitHub [#66](https://github.com/BitChillRSK/dca-contracts/pull/66). Specs only: the former Candidates C–F are now assigned as R13 and R31–R34, with implementation order and product gates recorded. Stacked on [#65](https://github.com/BitChillRSK/dca-contracts/pull/65). R22 deploy/CI moves to PR 28, R9 to PR 29, and R10 to PR 30.
- Assigned: [R33-uniswap-slippage-validation.md](./R33-uniswap-slippage-validation.md) (PR 23, GitHub [#67](https://github.com/BitChillRSK/dca-contracts/pull/67)). One constructor/setter slippage invariant. No product gate. Stacked on [#66](https://github.com/BitChillRSK/dca-contracts/pull/66).
- Assigned: [R13-operations-admin-lifecycle.md](./R13-operations-admin-lifecycle.md) (PR 24, GitHub [#68](https://github.com/BitChillRSK/dca-contracts/pull/68)). One owner-governance boundary, a typed form of the existing multi-swapper allowlist, an explicit one-shot route-class registry, and add-only versioned handler routes. Migration gate answered as option (a), manual exit/re-entry; idle is a route class rather than index `0`. Stacked on [#67](https://github.com/BitChillRSK/dca-contracts/pull/67).
- Assigned: [R31-handler-abi-trim.md](./R31-handler-abi-trim.md) (PR 25, GitHub [#69](https://github.com/BitChillRSK/dca-contracts/pull/69)). Remove redundant handler selectors and dead abstract-base token parameters before R9; fee-band mutation is atomic-only. Closes R13's class↔handler ERC-165 hole (`ITokenLending` on `assignTokenHandler`). Stacked on [#68](https://github.com/BitChillRSK/dca-contracts/pull/68).
- Assigned: [R34-dca-manager-abi.md](./R34-dca-manager-abi.md) (PR 26, GitHub [#70](https://github.com/BitChillRSK/dca-contracts/pull/70)). Canonical schedule reads and routing; mutation surface is intent-specific (`depositToken` / `setPurchaseAmount` / `setPurchasePeriod`), `updateDcaSchedule` removed. Stacked on [#69](https://github.com/BitChillRSK/dca-contracts/pull/69).
- Assigned: [R35-route-index-terminology.md](./R35-route-index-terminology.md) (PR 27, GitHub [#71](https://github.com/BitChillRSK/dca-contracts/pull/71)). Rename DcaManager `lendingProtocolIndex` → `routeIndex` after #70. Stacked on [#70](https://github.com/BitChillRSK/dca-contracts/pull/70).
- Assigned: [R32-internal-cleanup.md](./R32-internal-cleanup.md) (PR 28, GitHub [#72](https://github.com/BitChillRSK/dca-contracts/pull/72)). Behavior-preserving DcaManager/lending cleanup after the public surfaces settle. No product gate. Stacked on [#71](https://github.com/BitChillRSK/dca-contracts/pull/71).
- Assigned: [R22-deploy-ci.md](./R22-deploy-ci.md) (PR 29, GitHub [#73](https://github.com/BitChillRSK/dca-contracts/pull/73)). Production MoC map is idle / LayerBank / Sovryn; CI and `DeployMocSwaps` match. Stacked on [#72](https://github.com/BitChillRSK/dca-contracts/pull/72).
- Planning: GitHub [#74](https://github.com/BitChillRSK/dca-contracts/pull/74). Final specs and decisions: dead purchase selector, OZ5/ownership/authority hardening, both pause controls, schedule packing, Dex policy/routes, withdraw API, batcher, event/ABI freeze, and final natspec. Stacked on [#73](https://github.com/BitChillRSK/dca-contracts/pull/73). Every remaining prompt is gate-free; there is no optional-late queue.
- Assigned: [R39-remove-single-buy.md](./R39-remove-single-buy.md) (PR 30, GitHub [#75](https://github.com/BitChillRSK/dca-contracts/pull/75)). `buyRbtc` deleted from `DcaManager`, `PurchaseRbtc`, and both interfaces, along with the now-unreachable `PurchaseRbtc__RbtcPurchaseFailed`; a length-1 `batchBuyRbtc` is the one-schedule path. Frees 551 bytes on `DcaManager` and ~660 on every purchase handler (Dex margin 1,743 → 2,403 and 1,487 → 2,147). Measured one-schedule premium ~13–15k gas, accepted. No product gates. Stacked on [#74](https://github.com/BitChillRSK/dca-contracts/pull/74).
- Assigned: [R44-openzeppelin-5-upgrade.md](./R44-openzeppelin-5-upgrade.md) (PR 31, GitHub [#76](https://github.com/BitChillRSK/dca-contracts/pull/76)). OpenZeppelin pinned to tag `v5.7.0`: `ReentrancyGuard` moved to `utils/`, `Ownable(msg.sender)`, `safeApprove` → `forceApprove`, `Math.Rounding.Up` → `Ceil`, and OZ revert strings → custom errors (assertions kept exact via `test/utils/OzRevert.sol`). No first-party selector or event change. Every contract shrank — Dex margin 2,403 → 3,916 and 2,147 → 3,660 — and no production path got more expensive. OZ 5.x holds the reentrancy word in an ERC-7201 namespaced slot, so DcaManager's BitChill variables shift down one slot with no relative reordering; handlers are unchanged. No product gates. Stacked on [#75](https://github.com/BitChillRSK/dca-contracts/pull/75).
- Assigned: [R45-two-step-ownership.md](./R45-two-step-ownership.md) (PR 32, GitHub [#82](https://github.com/BitChillRSK/dca-contracts/pull/82)). Shared `BitChillOwnable` over `Ownable2Step`: Foundry broadcasts from an EOA; testnet that EOA is owner, mainnet proposes the Safe which must `acceptOwnership`. Renunciation is impossible. Dex constructors no longer call `onlyOwner` during init. Handlers grew ~387 bytes; Dex margin 3,916 → 3,515 and 3,660 → 3,259. No product gates. Stacked on [#76](https://github.com/BitChillRSK/dca-contracts/pull/76).
- Assigned: [R46-pin-operations-admin.md](./R46-pin-operations-admin.md) (PR 33, GitHub [#83](https://github.com/BitChillRSK/dca-contracts/pull/83)). `DcaManager` pins its constructor `OperationsAdmin` as immutable; `setOperationsAdmin` and `DcaManager__OperationsAdminUpdated` are gone. Runtime −187 bytes; the old `s_operationsAdmin` slot is removed. No product gates. Stacked on [#82](https://github.com/BitChillRSK/dca-contracts/pull/82).
- Out of band, merge first: CI toolchain pin, GitHub [#78](https://github.com/BitChillRSK/dca-contracts/pull/78). Branched off `main`, **not** stacked, so it lands ahead of this stack; rebase the stack onto it. `foundry-toolchain` and the forge version were both moving refs, and `stable` rolled to forge 1.8.0 on 2026-08-26, failing 10 `setUp()`s repo-wide. Reproduced on an unmodified pre-R44 commit, so it is the toolchain, not the stack.
- Not a relaunch item, tracked as GitHub issue [#79](https://github.com/BitChillRSK/dca-contracts/issues/79): migrating the test harness to Foundry 1.8.0 so [#78](https://github.com/BitChillRSK/dca-contracts/pull/78)'s pin can be raised. Deliberately an issue and **not** a spec here — it may never be done, and this queue holds only ordered, committed relaunch work. The issue carries the reproduction and the two hypotheses already disproven.
- Assigned: [R47-handler-address-uniqueness.md](./R47-handler-address-uniqueness.md) (PR 34, GitHub [#84](https://github.com/BitChillRSK/dca-contracts/pull/84)). An `OperationsAdmin` assigns each handler address exactly once; reuse reverts `OperationsAdmin__HandlerAddressAlreadyInUse` whatever the token or route class. Closes the cross-route hole where DcaManager locks principal per route while handler shares are per handler. `OperationsAdmin` runtime +225 bytes (4,625 → 4,850, margin 19,726 — pre-optimizer, see **Measurement basis**; 3,227 / 21,349 today); one appended mapping at slot 5, slots 0–4 untouched. No product gates. Stacked on [#83](https://github.com/BitChillRSK/dca-contracts/pull/83).
- Assigned: [R43-dex-path-review.md](./R43-dex-path-review.md) (PR 35, GitHub [#85](https://github.com/BitChillRSK/dca-contracts/pull/85)). Uniswap `amountOutMinimum` is scaled by the stablecoin's `decimals()`, so USDT0's 6-decimal floor is no longer 1e12 too small; more than 18 decimals reverts at deploy. The $1 peg, the execution-time oracle check, the absent router deadline, and the config-only safety check are recorded rather than changed. R39's two handoff questions are answered keep-as-is (batch still reverts on a share shortfall; fee still charged on the planned gross). Dex runtime +43 bytes (margins 3,472 and 3,216); one new deploy-time custom error. No product gates. Stacked on [#84](https://github.com/BitChillRSK/dca-contracts/pull/84).
- Assigned: [R41-reject-fot-deposits.md](./R41-reject-fot-deposits.md) (PR 36, GitHub [#86](https://github.com/BitChillRSK/dca-contracts/pull/86)). R21's hop-1 measurement stays, but the predicate tightens from `received == 0` to `received != requested`: a deposit now credits the full request or reverts `TokenHandler__DepositAmountMismatch(requested, received)` with a full rollback. `TokenHandler__ZeroStablecoinReceived` is gone (zero-received is that error with `received == 0`); the redeem-side `TokenLending__ZeroStablecoinReceived` is a different error and stays. Hop-2 lag coverage moved to a 1:1 stablecoin plus `MockKdocToken.setMintShortfallBps`. Handlers +1 byte; Dex margins 3,471 and 3,215. No product gates. Stacked on [#85](https://github.com/BitChillRSK/dca-contracts/pull/85).
- Assigned: [R40-update-purchase-period.md](./R40-update-purchase-period.md) (PR 37, GitHub [#87](https://github.com/BitChillRSK/dca-contracts/pull/87)). `setPurchaseAmount` / `setPurchasePeriod` become `updatePurchaseAmount` / `updatePurchasePeriod`, each emitting the value it replaced next to the new one (`PurchaseAmountUpdated`, `PurchasePeriodUpdated`, neither value indexed, so R9 inherits the frozen shape). The spec covered only the period setter; extended to both on 2026-08-28 because the two are symmetric and splitting them would break the ABI twice. Combined amount+period edits are still two transactions. `DcaManager` runtime +81 bytes (16,483 → 16,564, margin 8,012). No product gates. Stacked on [#86](https://github.com/BitChillRSK/dca-contracts/pull/86).
- Assigned: [R48-deposit-pause.md](./R48-deposit-pause.md) (PR 38, GitHub [#88](https://github.com/BitChillRSK/dca-contracts/pull/88)). Owner-only `setDepositsPaused(token, routeIndex, paused)` on `OperationsAdmin`: an assigned pair only, a real transition only, so every `OperationsAdmin__DepositsPauseSet` is a genuine state change. `DcaManager` consults it through one private `_handlerForDeposit` used by `depositToken` and `createDcaSchedule` alone, before any transfer; every other caller keeps `_handler`, so purchases, edits, deletion, and all three withdrawal paths ignore the pause by construction. `OperationsAdmin` runtime +921 bytes (4,850 → 5,771, margin 18,805); `DcaManager` +246 (16,564 → 16,810, margin 7,766); one mapping appended at slot 6, slots 0–5 untouched. No multi-pair form: a token-wide sweep is one transaction per pair and must skip pairs already paused, recorded in the spec's ops runbook note. No product gates. Stacked on [#87](https://github.com/BitChillRSK/dca-contracts/pull/87).
- Assigned: [R19-schedule-pause.md](./R19-schedule-pause.md) (PR 39, GitHub [#89](https://github.com/BitChillRSK/dca-contracts/pull/89)). User-owned `setSchedulePaused(token, scheduleIndex, scheduleId, paused)` keyed off `msg.sender`, so it reaches only the caller's own schedules. `DcaDetails` gains `bool paused`, appended after `routeIndex` for R18 to place. Only the purchase path reads it: `_rBtcPurchaseChecksEffects` reverts `DcaManager__SchedulePaused(user, token, scheduleId, scheduleIndex)` after id validation and before the balance debit and timestamp write, so one paused row fails the whole `batchBuyRbtc` with no sibling row keeping a debit or a consumed period. Deposits, edits, all three withdrawal paths, interest and rBTC claims, and deletion ignore the flag by construction — a pause can never strand funds. Unlike R48, a repeated write is a silent no-op rather than a revert: the caller is a user re-sending a click, not a Safe, and the early return gives the same "every event is a real transition" property. The flag lives in the struct, so swap-pop carries it with its schedule. `DcaManager` runtime +1,043 bytes (16,810 → 17,853, margin 6,723). The unpacked bool also costs the **protocol-paid** purchase path a seventh cold slot per row — `testSinglePurchase` 304,203 → 307,788 (+3,585), `testBatchPurchasesOneUser` +76,492 — which is why **R18 must follow immediately**: it packs `paused` in with `purchasePeriod`/`lastPurchaseTimestamp`/`routeIndex` and removes that read. Pause also makes the pre-existing cross-user batch DoS cheaper (an amount edit already reverts a batch identically, but a pause is free and reversible); mitigation is the bot's off-chain filter, and R42 bundling would widen it to every venue in a bundle. No product gates. Stacked on [#88](https://github.com/BitChillRSK/dca-contracts/pull/88).
- Assigned: [R49-schedule-struct-name.md](./R49-schedule-struct-name.md) (PR 40, GitHub [#90](https://github.com/BitChillRSK/dca-contracts/pull/90)). Rename-only `DcaDetails` → `DcaSchedule`: `Details` is a noise word, and every identifier around the struct already says "schedule", including the locals that hold it. Same correction class as R26 and R35. `DcaSettings` rejected — the struct carries protocol-written `tokenBalance` and `lastPurchaseTimestamp`, so naming it after its config half invites R6's stale-write-back hazard. Verified rather than asserted: `forge inspect methodIdentifiers` byte-identical, `storageLayout` identical slots with only the type label moving, and the full suite passes with no test edit beyond the type name. Deliberately before R18 so packing is written against the final name, and well before R9's freeze. No product gates. Stacked on [#89](https://github.com/BitChillRSK/dca-contracts/pull/89).
- Assigned: [R18-storage-packing.md](./R18-storage-packing.md) (PR 41, GitHub [#91](https://github.com/BitChillRSK/dca-contracts/pull/91)). `DcaSchedule` is three slots: two `uint128` amounts; `uint32` period + `uint48` timestamp + `uint32` route + `bool paused`; `bytes32 scheduleId`. External inputs stay `uint256`; overflow is OZ `SafeCast` before cash moves. `testSinglePurchase` 307,788 → 285,208; five-row batch −217k gas. Those are pre-optimizer figures (**Measurement basis**); the same two tests are 243,790 and 1,562,441 at today's head, and the packing win is a storage win that survives the optimizer. Handler share mappings stay `uint256`. No product gates. Stacked on [#90](https://github.com/BitChillRSK/dca-contracts/pull/90).
- Assigned: [R36-layerbank-usdrif-dex.md](./R36-layerbank-usdrif-dex.md) (PR 42, GitHub [#92](https://github.com/BitChillRSK/dca-contracts/pull/92)). `LayerBankErc20HandlerDex` deployed twice at dex index 1: USDRIF (replacing Tropykus) and USDT0 (new, 6-decimal min/fee). Runtime 21,578 bytes (EIP-170 margin 2,998) — pre-optimizer (**Measurement basis**); `LayerBankErc20HandlerDex` is 15,692 / 8,884 today. kUSDRIF mint is paused at the tip (`C2`). Stacked on [#91](https://github.com/BitChillRSK/dca-contracts/pull/91).
- Planning: [R50-packing-follow-up.md](./R50-packing-follow-up.md) (GitHub [#93](https://github.com/BitChillRSK/dca-contracts/pull/93)). Spec only: R50 inserted as PR 43. Stacked on [#92](https://github.com/BitChillRSK/dca-contracts/pull/92).
- Assigned: [R50-packing-follow-up.md](./R50-packing-follow-up.md) (PR 43, GitHub [#94](https://github.com/BitChillRSK/dca-contracts/pull/94)). `DcaSchedule` is two slots: the `keccak256(user, token, nonce)` id becomes the `uint64` nonce itself, and the struct is ordered by what a purchase *writes* — `tokenBalance` + `lastPurchaseTimestamp` + `paused` in slot 0, so the purchase's second write is a ~100-gas dirty write instead of a clean write to a second slot; the read-only half fills slot 1 exactly and `scheduleId` stays last. Ids start at 1 and `getSchedulesCreatedCount()` is the last one assigned; an exhausted counter reverts `SafeCastOverflowedUintDowncast` before the deposit is pulled. Fee settings are two slots (collector + both `uint16` rates, then two `uint128` bounds — the collector is declared first so the rates do not fall into the free bytes beside `_pendingOwner` and push it out), `OperationsAdmin` holds handler and deposit pause in one `TokenRoute` value, DcaManager's scalars share one slot with the nonce, and the two Uniswap percents share another. Every OperationsAdmin route-index argument is `toUint32()`-bounded, including `setDepositsPaused` and the getters — so a malformed oversized index in `withdrawAllAccumulated*` now reverts instead of being skipped. Handler per-user share/balance and rBTC mappings stay `uint256` and stay separate: merging them into one packed slot was tried and reverted, because the only shared parent is `StablecoinSource`, the funding-hook seam, which should not own rBTC state — the reasoning is recorded in the spec's **Out of scope** so it is not re-attempted as a drive-by. Create −30,306 gas, single purchase −9,012, five-row batch −77,936 (pre-optimizer; **Measurement basis**). Runtime grows (mask/shift on packed fields): `DcaManager` 22,518 (margin 2,058), `LayerBankErc20HandlerDex` 23,032 (margin 1,544). Pre-optimizer (**Measurement basis**): today those two are 13,767 / 10,809 and 15,692 / 8,884, so the mask/shift growth is real but the margins it was weighed against are not. Those are deploy sizes, not just a CI ceiling — every documented `--broadcast` command runs under `[profile.default]`, which is `via_ir = false`. No product gates. Stacked on [#93](https://github.com/BitChillRSK/dca-contracts/pull/93).
- Out of band, stacked on [#94](https://github.com/BitChillRSK/dca-contracts/pull/94): GitHub [#95](https://github.com/BitChillRSK/dca-contracts/pull/95). Not a relaunch item. Nit of [#85](https://github.com/BitChillRSK/dca-contracts/pull/85): one `ORACLE_DECIMALS` constant, no WRBTC `decimals()` check; and `depositToken` no longer returns the amount it already guaranteed.
- Assigned: [R37-retire-tropykus-live-paths.md](./R37-retire-tropykus-live-paths.md) (PR 44, GitHub [#96](https://github.com/BitChillRSK/dca-contracts/pull/96)). `TROPYKUS_INDEX` lives in `test/Constants.sol`, which no `script/` file imports, so a deploy script naming a Tropykus route fails to compile rather than failing review; `TROPYKUS_STRING` stays in `script/Constants.sol` because the helper configs select mocks with it. `DeployDexSwaps`' live branch drops the Tropykus `registerRoute` / deploy / assign and reverts on `Protocol.TROPYKUS`, matching `DeployMocSwaps` — the live dex map is LayerBank (USDRIF / USDT0) plus Sovryn (DOC), and index 4 stays burned. No `src/` change and no ABI change. `script/tropykus-legacy/` was **not** created: R36 repurposed `DeployUsdrifHandler` wholesale, so no Tropykus-only script remains to move and every other script is shared with a live path — compile scope carries the `src/` ↔ `script/` split instead. `UsdrifHelperConfig`'s write-only `kUsdrifTokenAddress` is removed as R36's comment promised. Passing counts are identical in all six measured lanes; each gains one test and one skip from `test_dexLive_revertsForTropykus`, and `dex-tropykus` trades the live-map test for it. No product gates. Stacked on [#95](https://github.com/BitChillRSK/dca-contracts/pull/95).
- Assigned: [R38-withdraw-all-route-pairs.md](./R38-withdraw-all-route-pairs.md) (PR 45, GitHub [#97](https://github.com/BitChillRSK/dca-contracts/pull/97)). `withdrawAllAccumulatedInterest` and `withdrawAllAccumulatedRbtc` take positional `(token, routeIndex)` pairs instead of a `tokens × routeIndexes` grid, so a user holding two tokens at two different routes stops calling the handlers on the combinations they do not hold. Same selectors, changed argument meaning — deliberate, with no legacy alias, which R34's no-dual-window cutover makes acceptable. Empty input now reverts `DcaManager__EmptyWithdrawalArrays` instead of succeeding silently, mismatched lengths revert `DcaManager__ArraysLengthMismatch` (the same error `batchBuyRbtc` uses), and every existing skip (unassigned handler, non-lending route, zero rBTC balance) is unchanged, so over-listing pairs is still safe. `isLendingRoute` runs once per pair rather than once per combination. The mixed case is proven by call counts on a *registered* cross-pair handler — an unassigned one would be skipped on the zero-address check and prove nothing — and that test fails against the old nested loop. `DcaManager` runtime 22,470 → 22,578 bytes (margin 2,106 → 1,998); no handler changed. Pre-optimizer (**Measurement basis**); `DcaManager` is 13,767 / 10,809 today. Frontend follow-up: [front-end#21](https://github.com/BitChillRSK/front-end/issues/21) ([#13](https://github.com/BitChillRSK/front-end/issues/13) covered the same ground and is closed). No product gates. Stacked on [#96](https://github.com/BitChillRSK/dca-contracts/pull/96).
- Assigned: [R42-swapper-batcher.md](./R42-swapper-batcher.md) (PR 46, GitHub [#98](https://github.com/BitChillRSK/dca-contracts/pull/98)). `SwapperBatcher.batchBuyRbtcGroups` forwards several `DcaManager.batchBuyRbtc` groups in one transaction without adding `multicall` to the manager or taking custody of user funds. Callers and the batcher itself authenticate against the same OperationsAdmin swapper list (without the outer check, a random EOA could consume one due schedule and revert the bot's atomic bundle). Failures stay all-or-nothing, re-confirmed against R19. The bot EOA stays allowlisted for per-handler retries. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5510363419). No product gates. Stacked on [#97](https://github.com/BitChillRSK/dca-contracts/pull/97).
- Assigned: [R9-event-indexing.md](./R9-event-indexing.md) (PR 47, GitHub [#99](https://github.com/BitChillRSK/dca-contracts/pull/99)). Event/indexing/share/fee audit and ABI freeze. Every existing scalar address and `scheduleId` is indexed, and nothing else is; dynamic address arrays and encoded paths stay in data. `DcaScheduleDeleted` now indexes user, token, and scheduleId; `PurchaseUniswap_OracleUpdated` indexes both oracle addresses. Lending handlers emit `TokenLending__UserSharesUpdated` after every virtual share mint or burn; purchase fees emit `FeeHandler__FeeTransferred` when non-zero. `SchedulePauseSet` stays user+scheduleId (no token). Cutover: [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5510369129), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5510369816). No product gates. Stacked on [#98](https://github.com/BitChillRSK/dca-contracts/pull/98).
- Assigned: [R10-natspec.md](./R10-natspec.md) (PR 48, GitHub [#100](https://github.com/BitChillRSK/dca-contracts/pull/100)). First-party natspec/comments pass after the ABI freeze. User-facing docs on interfaces; `@inheritdoc` in implementations. No behavior change. No product gates. Stacked on [#99](https://github.com/BitChillRSK/dca-contracts/pull/99).
- Assigned: [R42-swapper-batcher.md](./R42-swapper-batcher.md) integration follow-up (PR 49, GitHub [#101](https://github.com/BitChillRSK/dca-contracts/pull/101)). `DcaManager.batchBuyRbtcAcrossHandlers` replaces the standalone batcher, authenticates once, and purchases each handler's batch through the same one-handler helper. The retry entry is `batchBuyRbtc(Batch)`; the former six-argument selector does not ship. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5510363419), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5510369816), and [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5510369129). Stacked on [#100](https://github.com/BitChillRSK/dca-contracts/pull/100).
- Planning review: GitHub [#102](https://github.com/BitChillRSK/dca-contracts/pull/102) finalizes the R51/R52 specs and targets their implementation at GitHub #103 and #104. Stacked on [#101](https://github.com/BitChillRSK/dca-contracts/pull/101).
- Assigned: [R51-per-batch-min-rbtc-out.md](./R51-per-batch-min-rbtc-out.md) (PR 50, GitHub [#103](https://github.com/BitChillRSK/dca-contracts/pull/103)). Appends aggregate `minRbtcOut` to PR 101's existing `Batch` tuple and enforce it against measured output in the shared handler pipeline. Units are always WRBTC/native-rBTC wei; `0` preserves current behavior, while the production bot sends a fresh quote-derived minimum for Dex. PR 103 keeps the 99.5% / 95% source defaults, records one fork table and opens the off-chain issues, but does not wait for ongoing calibration. Before relaunch the Safe chooses the tightest durable per-handler oracle backstop that clears the measured envelope with quantified headroom, records its maximum-loss budget, and re-locks the safety check to the same value in a separate Safe execution. Weekly execution is automatic quote/retry/split/failover, and whichever of the static oracle floor or dynamic caller minimum is stricter wins; monitoring alerts on a zero Dex minimum while exempting MoC. Swapper-bot remains the sole signer and consumes reusable/read-only route simulation logic derived from `rsk-uniswap-pools`. Implemented as specified except for one corrected instruction: both DcaManager purchase selectors change (`0x8e78f242`, `0x6885b843`) and the handler's becomes `0x0a0c5350`, but the spec's mandated `_creditBuyers` extraction is **not** needed and was not shipped. The added parameter does overflow the no-IR stack, by exactly one slot, and scoping `aggregatedFee` to the block that pays the fee releases it — so the loop stays inline and keeps its cached `buyers.length`, measurably cheaper (−87 gas single purchase, −416 five-row batch) and 17 bytes smaller per Dex handler than extracting. The loop also holds its twice-read `netStablecoinAmountsToSpend[i]` / `buyers[i]` in locals, worth 74 gas per row and 42 bytes per handler — much the largest of the three effects; the block scope is −28 / −298, and the length cache costs ~12 gas per call to save 3.00 per row. Enabling the legacy optimizer does not relieve stack-too-deep (checked); only via-IR does, and that stays out of bounds for EIP-170 decisions. `DcaManager` +20 bytes (23,703, margin 873), each Dex handler +57 (LayerBank 23,475 / margin 1,101; Sovryn 23,015 / 1,561; Tropykus 23,271 / 1,305); `OperationsAdmin` unchanged. Those sizes are pre-optimizer (**Measurement basis**): at today's head `DcaManager` is 13,767 (margin 10,809) and the Dex leaves 15,479–15,692 (8,884–9,097), so the 893-byte manager margin the `_creditBuyers` discussion weighed is gone; the stack-too-deep constraint it actually turned on is not, and the optimizer does not relieve it. The gating fork table is `make probe-dex-quote-floor` at pinned block 9198813, and its findings are negative for two of three routes: Sovryn DOC is **starved** (the DOC/rUSDT 0.05% pool held ~1.48 rUSDT, so every size partially fills at ~0.19 DOC and splitting cannot help), LayerBank USDRIF misses 99.5% by 16 bps at the $25 minimum and 23 bps at $1,000, and only LayerBank USDT0 clears — by 40 and 34 bps at those sizes, failing $100,000 by 466 bps on price impact. Per the spec those negatives are valid PR evidence and do not block merge. The DOC result also settled a route question: **Sovryn DOC is not a shipped Dex route and must never become one** — DOC buys rBTC only through MoC redemption, and may appear in a Uniswap path only as an intermediate hop. The shipped Dex set is LayerBank USDRIF and LayerBank USDT0; this supersedes the live-map wording in the R37 (PR 44) entry above. The DOC Dex handler stays test-only legacy, as Tropykus did after R37. Not fixed here and tracked as [R57](./R57-close-doc-dex-deploy-hole.md): `DeployDexSwaps`' live branch could still construct that handler when `STABLECOIN_TYPE=DOC`, so the rule was documentation-only in this PR. Closed by [#106](https://github.com/BitChillRSK/dca-contracts/pull/106). Also recorded, not changed: `DexHelperConfig` comments both hops as "rUSDT", but the USDRIF hop `0xAf368c…` is 6-decimal `USDT`, a different token from the 18-decimal `rUSDT` `0xef2134…` on the DOC path; re-approving a path is R52's surface. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5477257073), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5477260356), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5477262287), and the quote engine [rsk-uniswap-pools#2](https://github.com/BitChillRSK/rsk-uniswap-pools/issues/2); `data-api` needs none (it does not encode `Batch`). No contract-PR product gates; calibration and one-time cutover approval gate Dex relaunch. Stacked on [#102](https://github.com/BitChillRSK/dca-contracts/pull/102).
- Assigned: [R52-allowlisted-dex-path-failover.md](./R52-allowlisted-dex-path-failover.md) (PR 51, GitHub [#104](https://github.com/BitChillRSK/dca-contracts/pull/104)). Handler-local exact-path allowlist on `PurchaseUniswap`; constructor self-allowlists the initial path (emits `NewPathSet` + `PurchasePathAllowedSet`); handler owner approves later paths; an OperationsAdmin swapper or that handler owner may activate only an allowed path. Purchases do not SLOAD the allowlist. Default profile is optimizer-on / no-IR (`optimizer = true`, `optimizer_runs = 200`, `via_ir = false`). Runtime (EIP-170 24,576): `DcaManager` 13,767 (margin 10,809); `OperationsAdmin` 3,227 (21,349); `LayerBankErc20HandlerDex` 15,592 (8,984); `SovrynErc20HandlerDex` 15,379 (9,197); `TropykusErc20HandlerDex` 15,523 (9,053). Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5510363419), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5510369129), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5510369816), [rsk-uniswap-pools#2](https://github.com/BitChillRSK/rsk-uniswap-pools/issues/2). Optimizer-on proof **passed** 2026-09-01: CREATE [0x3a63dc…](https://rootstock-testnet.blockscout.com/tx/0x3a63dc2458142cca09144a3f290ed6d996780c616acb8adbdf15f57f736ff5bc) verified [`OperationsAdmin` `0x8D7B64ed7Ef7B862bB52c7381b9246d2669a4FAD`](https://rootstock-testnet.blockscout.com/address/0x8D7B64ed7Ef7B862bB52c7381b9246d2669a4FAD). Ready for human review. R53's optimizer-baseline work is absorbed here; remaining R54 (schedule top-up) and R55 (solx / via-IR / `ZeroTokenPurchaseUniswap`) live in stacked [#105](https://github.com/BitChillRSK/dca-contracts/pull/105), which needs rebase after this PR. Stacked on [#103](https://github.com/BitChillRSK/dca-contracts/pull/103).
- Planning review: GitHub [#105](https://github.com/BitChillRSK/dca-contracts/pull/105) queues [R53](./R53-optimizer-baseline.md) / [R54](./R54-schedule-top-up-from-interest.md) / [R55](./R55-solx-and-ir-evaluation.md) / [R56](./R56-dex-oracle-floor-is-safety-check.md) and reopens licensing. Comment-only Foundry section banners on five `src/` bases; no Solidity behavior. Stacked on [#104](https://github.com/BitChillRSK/dca-contracts/pull/104).
- Assigned: [R57-close-doc-dex-deploy-hole.md](./R57-close-doc-dex-deploy-hole.md) (PR 52, GitHub [#106](https://github.com/BitChillRSK/dca-contracts/pull/106)). Live `DeployDexSwaps` reverts on DOC (`"DOC is not on the production dex map"`) before constructing a Dex handler, so an unset `STABLECOIN_TYPE` can no longer burn `(DOC, SOVRYN_INDEX)`. Sovryn stays registered as a lending route. Local/fork DOC Dex handlers stay for `make dex-sovryn` and `ComparePurchaseMethods`. No `src/` change. Stacked on [#105](https://github.com/BitChillRSK/dca-contracts/pull/105).
- Assigned: [R56-dex-oracle-floor-is-safety-check.md](./R56-dex-oracle-floor-is-safety-check.md) (GitHub [#107](https://github.com/BitChillRSK/dca-contracts/pull/107)). Uniswap `amountOutMinimum` is `max(s_amountOutMinimumPercent floor, minRbtcOut)`. The slippage ABI is unchanged: both fractions, both setters/getters/events, and the `safetyCheck <= percent <= 100%` band all stay. What changes is the deploy default floor (99.5% -> 97%, derived from `make probe-dex-quote-floor` at block 9198813, where the worst realistic fill was LayerBank USDRIF at 99.27%) and the fact that a too-tight `minRbtcOut` now reverts in SwapRouter02 rather than raising `PurchaseRbtc__BelowSwapperMinimum`. Optimized Dex: LayerBank 15,692 B (8,884 margin), Sovryn 15,479 B (9,097 margin), Tropykus 15,623 B (8,953 margin). Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5510363419), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5510369816), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5510369129). Stacked on [#106](https://github.com/BitChillRSK/dca-contracts/pull/106).
- Assigned: [R58-split-script-test-constants.md](./R58-split-script-test-constants.md) (PR 54, GitHub [#108](https://github.com/BitChillRSK/dca-contracts/pull/108)). Test-only constants leave `script/Constants.sol`: harness account labels, the purchase-assertion slippage tolerances, the 1e18 share-rate scale, the fee divisor, and the fork holders now live in `test/Constants.sol`, and 30 test files import that barrel instead. `BTC_PRICE` stays in script (the dex and USDRIF helper configs pass it to `MockSwapRouter02`), as do `OWNER_STRING` / `FEE_COLLECTOR_STRING` (`DeployBase` derives the local and fork accounts from them). `RESERVED_MOC_LENDING_INDEX` becomes a comment rather than moving — it reserves index 3 on the production MoC map, and an unused constant is what invites a script to register it. Fork holders consolidate on the `_TESTNET` suffix. The R37 guard is strengthened, not relaxed: it holds today only because `script/*.s.sol` imports mocks with the named `import {Mock} from …` form, so `MockMocOracle` is repointed back at `script/Constants.sol` and `MockSwapRouter02`'s dead import deleted — no mock a deploy script constructs can see `TROPYKUS_INDEX` under any import form. No `src/`, ABI, or deploy-value change; no consumer cutover. Stacked on [#107](https://github.com/BitChillRSK/dca-contracts/pull/107).
- Assigned: [R53-optimizer-baseline.md](./R53-optimizer-baseline.md) (PR 55, GitHub [#109](https://github.com/BitChillRSK/dca-contracts/pull/109)). Documentation only, no `src/`, `script/`, `test/`, or build-config change. #104 turned the optimizer on and corrected its own figures; this PR corrects every other recorded size, EIP-170 margin, and gas number, which had all been measured with `optimizer = false`. The fresh full-build table and the two hot-path gas figures now live in one place — **Measurement basis** above — and each historical entry or spec labels its pre-#104 number beside it and links there instead of carrying a copy of the table. Where a shipped PR chose something because of a size, the entry stands and the corrected figure is appended. Measured at the R58 head, not by rebuilding each historical commit, which the annotations state: two figures moved since #104 for source reasons, the three Dex leaves by +100 B (R56) and the two gas tests to 243,790 / 1,562,441 from 243,817 / 1,562,720. Six decisions argued from bytecode scarcity were listed in the spec's [flag table](./R53-optimizer-baseline.md#bytecode-scarcity-decisions-closed); **none was re-judged in R53**. [#111](https://github.com/BitChillRSK/dca-contracts/pull/111) later closed that queue: R31 / R39 / R42 / R51 shipped and stand on non-size merits; R13 cooperative migration and R50's merged per-user slot stay decided against. One argument is narrowed rather than reopened: R51's `_creditBuyers` reasoning turned on no-IR stack-too-deep, which the optimizer does not relieve (R51 checked, and it still holds with the optimizer on), so only its 17-bytes-per-handler half was budget-driven and via-IR (R55) is what would reopen it. Files beyond the spec's list, all one-line labels except R13's appended correction: R9, R13, R30, R34, R36, R38, R39, R43, R44, R45, R46, R47, R55. Cutover: none. Stacked on [#108](https://github.com/BitChillRSK/dca-contracts/pull/108).
- Assigned: [R54-schedule-top-up-from-interest.md](./R54-schedule-top-up-from-interest.md) (PR 56, GitHub [#110](https://github.com/BitChillRSK/dca-contracts/pull/110)). `DcaManager.topUpFromInterest(token, scheduleIndex, scheduleId, amount)` credits accrued lending interest to one schedule's `tokenBalance`. `DcaManager`-only: no handler call that moves cash, no token movement, no redeem-then-remint — the funds already sit in the lending position, so the credit pays no redemption fee (Sovryn's SIP-0094 exit fee is what the withdraw-then-deposit round trip costs). The credit is bounded above by the same `getAccruedInterest` expression the withdrawal path pays out, so the route's summed principal lands on the position's value and never above it. Two of the three product decisions went **against** the spec's recommendations: the R48 deposit pause does **not** block a top-up (`_handler`, not `_handlerForDeposit`) because that pause stops new funds entering a route and interest already in the route is not new funds; and the function takes an `amount`, so one pot of route interest can feed two schedules without the fee-paying round trip, bounded below by having to raise the balance past its next whole purchase (a schedule holding 10.5 purchases needs half a purchase amount, not a full one). The name is `topUpFromInterest`, as recommended. Out of the spec's scope but taken here: the accrued figure is split into two readers, because R54 turns it from something a user reads into something a user spends against, and the two jobs want different rates. `ITokenLending.getAccruedInterest` stays the **spendable ceiling** at the market's current rate (`onlyDcaManager`, so its non-view mutability never reaches a generated client); a new `ITokenLending.quoteAccruedInterest` is the **display quote** at the stored rate and keeps `DcaManager.getInterestAccrued` a `view`. A stored rate only ever trails a current one, so the quote is never above the ceiling; passing it therefore satisfies the upper bound, but can still fail the independent minimum requiring the credit to cross the schedule's next purchase boundary. The first attempt instead let the mutability propagate and dropped `view` from `DcaManager.getInterestAccrued`, which is not additive — generated clients route non-constant functions through write bindings, and in ethers v6 that sends a transaction to read a balance — to buy behaviour only the legacy Tropykus adapter can exhibit, and Tropykus is on neither production map. ABI: **additive**, one function, one event `DcaManager__ScheduleToppedUpFromInterest`, three errors, no existing selector, mutability, or indexed field moved; `ITokenLending`'s ERC-165 id changes with its new function, which is invisible because handlers and `OperationsAdmin` ship together. `DcaManager` 13,767 → 14,542 B (margin 10,809 → 10,034), +775 against the probe's +600, which had neither the `amount` nor its bounds; the split cost 1 B of that, since a `view` external call compiles to `staticcall`, and moved the rest to the lending leaves (+136 to +229 B against margins of 8,748 B and up; `IdleDocHandlerMoc` is byte-identical). 18 tests in `test/unit/TopUpFromInterestTest.t.sol` plus a top-up action and coverage guard in the stateful fuzz harness (it is the only action that raises principal with no matching deposit, so it is what stresses `invariant_totalDepositedTokensMatchesLendingProtocol` and its 100-wei tolerance; 64×512 passes with zero reverts). Green on all three lending lanes and on `make fork-sovryn` / `make fork-tropykus`; the unit tests size their amounts from what each lane actually accrued, since a fork pays ~8 DOC where the mocks pay ~55. Recorded, not fixed: `invariant_interestOnlyIncreases` asserts `assertGe(uint256, 0)` and encodes nothing. Cutover: [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5512733974), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5512737902), [data-api#9](https://github.com/BitChillRSK/data-api/issues/9#issuecomment-5512738181), and `metrics-dashboard` — a balance rising with no matching ERC-20 transfer is otherwise indistinguishable from a deposit, and reported yield falling while principal rises double-counts interest in deposits-vs-yield reporting; `swapper-bot` needs none. Stacked on [#109](https://github.com/BitChillRSK/dca-contracts/pull/109).
- Planning review: GitHub [#111](https://github.com/BitChillRSK/dca-contracts/pull/111) adds the implementation-ready [R59](./R59-uniswap-exact-consumption.md) exact-consumption invariant, including suggested Solidity edits, rollback tests, and gas/runtime ceilings. Order updated so R59 precedes R55; no Solidity behavior changes here. Also closes the R53 bytecode-scarcity follow-up queue (shipped choices stand; R13 / R50 stay decided against) — R53 had left that table sounding open even though every row was already settled. Stacked on [#110](https://github.com/BitChillRSK/dca-contracts/pull/110).
- Assigned: [R59-uniswap-exact-consumption.md](./R59-uniswap-exact-consumption.md) (PR 57, GitHub [#112](https://github.com/BitChillRSK/dca-contracts/pull/112)). `PurchaseUniswap` brackets `exactInput` with balance deltas: the handler must end exactly `stablecoinAmount` poorer, and SwapRouter02 must end with every active intermediate-token balance equal to its own pre-swap value, so router dust cannot grief a purchase and a hop that stopped short cannot strand tokens outside our custody. Either mismatch reverts the whole batch — pools, router, fees and schedule effects together — which is the entire recovery mechanism; no refund, sweep, path decoding, assembly, or router-return accounting. The active path's intermediate tokens are stored beside `s_swapPath` and written by the same statement pair, so failover cannot leave the previous route's metadata behind. ABI: **additive**, two diagnostic errors on `IPurchaseUniswap`, no existing selector, event, indexed field, or mutability moved. Shipped as specified except for one deviation, which is what clears the ceilings: the spec's suggested shape costs **+1,163 B** per Dex leaf, 363 over its own 800-byte gate, and the largest piece of that is six inline copies of the same `balanceOf` encode/staticcall/decode, so the purchase's six balance reads share one private `_balanceOf` — 508 B cheaper for a JUMP per read. Ablation of the spec shape: input-spent check 298 B, the memory→storage array copy in `_setPurchasePath` 158 B, the two purchase-time loops plus the second error 593 B. Measured `7b18781` → `614cdf7` under the #104 pin ([Measurement basis](#measurement-basis)): **+655 B** on each Dex leaf (LayerBank 16,483 / margin 8,093; Sovryn 16,270 / 8,306; Tropykus 16,414 / 8,162), `DcaManager`, `OperationsAdmin` and `IdleDocHandlerMoc` byte-identical, and **+4,891 gas** direct / **+14,135 gas** through one intermediate on `testSinglePurchase`, the all-cold length-1 batch, against ceilings of 8,000 and 15,000. Also measured and rejected: indexing the array from storage in both loops saves a further 44 B and 45 gas on the multihop path but costs 226 gas on the direct path, which is a live route. 8 tests in `test/unit/PurchaseUniswapExactConsumptionTest.t.sol` — both path shapes on both lanes, a first-hop and a later-hop partial fill that each still clear `amountOutMinimum`, full-rollback assertions on schedule balance, timestamp, handler and router token balances, fee transfer and buyer rBTC credit, and router dust that must not block a purchase — plus one local-only path-failover case in `DexPathFailoverTest`. Files beyond the spec: **both** Anvil dex configs used `makeAddr("rUSDT")` for the intermediate token, and the purchase now reads that token's `balanceOf` on the router, where a high-level call to a codeless address reverts — `script/DexHelperConfig.s.sol` and `script/UsdrifHelperConfig.s.sol` each deploy a real `MockStablecoin` there instead, with no live-network branch touched. The second one was invisible to the existing deployment tests, which read constructor state and never buy, so every locally deployed `DeployUsdrifHandler` route would have reverted mid-swap; `test/unit/deployment/NewHandlerDeploymentTest.t.sol` gains one purchase through that route, which fails with "call to non-contract address" without the fix. Green on `make check`, `make fork-sovryn`, `make fork-tropykus`, `make fork-dex-path`, and — the real-router evidence — `SWAP_TYPE=dexSwaps STABLECOIN_TYPE=USDRIF make fork-layerbank`, where the configured-path case buys through the live SwapRouter02 and the USDRIF/rUSDT/WRBTC pools. That lane's only failures are `SchedulePackingTest`'s three pre-existing `SafeCast` overflows, which fail identically at the base commit (the impersonated whale's balance does not fit the packed `uint128`). Two success cases that activate a route the test invents are local-only, since no such pool exists on a fork, and the "router gained exactly the net amount" assertion is local-only too — the real router forwards the input into the pool and ends holding none of it. Cutover: [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5517293835) (`abi.json`, and a repeat occurrence is alert-worthy) and [swapper-bot#5](https://github.com/BitChillRSK/swapper-bot/issues/5#issuecomment-5517301241) (a new batch-reverting condition: re-quote, split, or fail the path over rather than retry); `front-end`, `data-api` and `metrics-dashboard` need none, since the errors surface only on `batchBuyRbtc`. No product gates. Stacked on [#111](https://github.com/BitChillRSK/dca-contracts/pull/111).
- Assigned: [R55-solx-and-ir-evaluation.md](./R55-solx-and-ir-evaluation.md) (PR 58, GitHub [#113](https://github.com/BitChillRSK/dca-contracts/pull/113)). Documentation only: no compiler setting, `src/`, `test/`, `script/` or CI change. **Recommendation: keep stock solc, no IR** — the #104 pin. Four configurations on one host at `260d7ef`, every number against the optimized no-IR baseline, with a stock-solc-0.8.34 control on the pragma-downgraded copy that reproduces the baseline byte for byte and gas to the unit. solx **v0.1.8** (sha256 `5eda882c…4ec2`) is the fastest measured — `DcaManager.batchBuyRbtc` 188,806 against 202,530 (−6.8%), `testSinglePurchase` 222,031 (−8.9%) — and all seven `make check` lanes pass under it with counts identical to the control, invariants included. It still loses: its Solidity front end is a **fork** at `0.8.34+commit.91fef221` (upstream 0.8.34 is `80d5c536`) while all 143 first-party files pin `pragma solidity 0.8.36;`, so it cannot compile this repo without an out-of-scope `src/` change and a re-proof of R23; it stamps `solx:0.1.8;solc:0.8.34` into the CBOR metadata; and both Rootstock Blockscout instances advertise only stock Solidity and Vyper compilers, so the bytecode would be unverifiable. Its own README says beta, testing and experimentation only. `via_ir` is stock solc and keeps the verification story, but buys only −2.6% on `batchBuyRbtc` (−2.0% / −4.2% on the two hot-path tests, ~20–26% smaller runtime that nothing needs) and costs test work: `ZeroTokenPurchaseUniswap` fails with the predicted error 1284, and a **second, previously unrecorded** blocker turned up — `test/unit/RbtcWithdrawalTest.t.sol` hits Yul stack-too-deep in the shared harness's `makeSeveralPurchasesWithSeveralSchedules`; `forge test --match-*` hides both by compiling sparsely. With both skipped one test still fails on six lanes, and it reduces to a `block.timestamp` cached across `vm.warp` being rematerialised by the Yul optimizer — a harness hazard, not a production miscompilation, and the clearest evidence in the evaluation that a green suite says little about a code generator. `additional_compiler_profiles` + `compilation_restrictions` compiles the 1284 case without touching the test, at the price of compiling every production contract twice and having that test exercise legacy bytecode while production ships IR. ABI and storage layout are compiler-invariant (checked, not assumed). The Rootstock testnet deploy in the spec's scope was **not** performed and is recorded as such: `AGENTS.md` forbids broadcasting, solx cannot compile the tree at 0.8.36, and the explorers' verifier configuration answers the question. Cutover: none. Stacked on [#112](https://github.com/BitChillRSK/dca-contracts/pull/112).
- Reopened 2026-09-03 as [R60-src-only-via-ir.md](./R60-src-only-via-ir.md), implemented same day except
  one item. R55 weighed `via_ir` as one project-wide setting; every cost it found
  (`ZeroTokenPurchaseUniswap` error 1284, the `RbtcWithdrawalTest.t.sol` stack-too-deep, the
  `block.timestamp`/`vm.warp` rematerialization) is a test-file failure, not a `src/` one. The first design
  considered — `via_ir` on `src/**` only, `test/**`/`script/**` kept legacy via
  `compilation_restrictions` — turned out not to satisfy "run the whole suite against the shipped
  bytecode": a legacy-compiled test's `new DcaManager(...)` links against a **second, legacy-codegen
  build** of that contract (confirmed by trace), not the IR one `forge script` would deploy. So the design
  changed to a `[profile.deploy]` (`FOUNDRY_PROFILE=deploy`, new `make check-deploy`/`make build-deploy`
  targets) that compiles `via_ir = true` across `src/`, `test/`, and `script/` with exactly **one** file
  excluded — `test/unit/ZeroTokenPurchaseUniswapTest.sol` (split out of `PurchaseUniswapSettingsTest.sol`),
  because that contract's constructor is deliberately, provably always-reverting and trips a known
  upstream solc/via-IR limitation ([ethereum/solidity#11642](https://github.com/ethereum/solidity/issues/11642))
  that no `src/` change can fix. The withdrawal-harness stack-too-deep was fixed by splitting
  `DcaDappTest.makeSeveralPurchasesWithSeveralSchedules`'s inner loop into a private helper; the
  `vm.warp` fuzz failure was fixed by moving the cached pre-warp timestamp from a stack local (which the
  Yul optimizer can rematerialize across a cheatcode-driven clock change) into a private storage variable.
  Both fixes are assertion-identical to what they replaced; no `src/` file changed. `make check-deploy`
  passes all seven lanes with the same counts R55 recorded for its project-wide via-IR run, and a
  from-scratch `--sizes` build reproduces R55's `DcaManager` IR size (11,712 B) with no stray
  `.legacy-codegen` duplicate. Default `make check`/`make ci` are unchanged and still fast
  (`via_ir=false`). Does not reopen solx (still unverifiable on Rootstock, still can't compile the pinned
  pragma). **Outstanding:** the Rootstock testnet deploy + Blockscout verification of the `via_ir`-compiled
  bytecode needs a human operator (`AGENTS.md` forbids broadcasting from an agent session) — this is the
  one thing R55 also deliberately skipped, and it is the only unproven link in R60's chain.
- Assigned: [R61-src-comment-and-abi-cleanup.md](./R61-src-comment-and-abi-cleanup.md) (PR 60, GitHub [#115](https://github.com/BitChillRSK/dca-contracts/pull/115)). Not from the ordered queue — a cleanup item raised after R60 from an audit of all 42 `src/` files. Comment **volume** was not the finding: across the relaunch `src/` went 3,902 → 4,552 lines (+278 code, +372 comment), a comment:code ratio of 0.71 → 0.78, in line with comparable protocols. Four real defects: `IFeeHandler.getFeeCollectorAddress()` was missing `view`, so the interface's published ABI said `nonpayable` while the implementation was `view` (Solidity permits an override to narrow mutability, so it compiled). Blast radius measured rather than assumed: of the 33 built artifacts declaring the function, exactly one — `IFeeHandler.json` — said `nonpayable`; `FeeHandler` and every concrete handler already emitted `view`, since solc emits each contract's own mutability. No deployed contract's ABI ever advertised `nonpayable`, so the defect reaches only a client generated from the standalone interface, which gets a write binding for a getter; the `DcaSchedule` packing NatSpec still claimed "Without IR, those updates remain two `SSTORE`s", which after R60's `[profile.deploy] via_ir = true` describes the profile that does **not** deploy; `IStablecoin` was test-only (no reference from `src/` or `script/`, only the four mocks, and its own NatSpec said so) but sat in `src/interfaces/`, and moves to `test/interfaces/`; and `deleteDcaSchedule` was the only external function writing `s_dcaSchedules` whose `nonReentrant` was not the first modifier, which matters because invariant 6 is meant to be checkable with grep. That reorder is the PR's one executable change, and it is not quite behavior-neutral: a reentrant call carrying an invalid schedule index now reverts with `ReentrancyGuardReentrantCall` rather than `DcaManager__InexistentScheduleIndex`. Which revert an already-rejected call returns is the whole of its observable effect. The comment layer is rewritten in one voice: stacked `@dev` blocks collapse to one paragraph per function (four consecutive tags meant four review rounds each appending a defense — `topUpFromInterest` carried four in `DcaManager` plus a 24-line block in `IDcaManager`, 42 lines across two files for a 33-line function, explaining the ceiling-vs-quote distinction twice; it now lives once, on `@param amount`); 11 NatSpec tags sitting inside function bodies, where they render nowhere, become plain comments; inline comments restating the NatSpec above them are deleted; gas figures and editorializing give way to the durable reason (the `~74 gas per row` measurement was taken under `via_ir=false`, which R60 made the non-shipping profile); and internal vocabulary that escaped the `R\d+` grep — "invariant 1 is handler cash", "Individual bound/rate setters were removed", "the max-schedules bound has sat here since the count check was fixed" — is replaced by the standalone reason. `ICoinPairPrice` drops 22 of 25 members that no BitChill contract or test calls, plus a function-pointer struct and four commented-out vendor declarations, keeping the price reads and the attribution; safe because no mock declares `is ICoinPairPrice`. ABI: `getFeeCollectorAddress` `nonpayable` → `view`, no selector, event, or indexed field moved. **Bytecode proof:** metadata-stripped runtime unchanged on all nine deployable contracts except `DcaManager` 14,489 → 14,492 B stripped (14,542 → 14,545 with metadata, +3), isolated by rebuilding with only the modifier reorder reverted, which returns exactly the baseline — so every comment change and the `view` annotation are provably zero-bytecode. Complete `deployedBytecode` differs on all nine, because Solidity's CBOR metadata hash covers the comments; `forge build --sizes` shows the size did not move but cannot show the bytes did not, which is why the comparison is the metadata-stripped one R10 established. Green on `make check`, `make fork-sovryn`, `make fork-tropykus`, and `make check-deploy` (the via-IR profile that ships). Cutover: none filed, and the artifact audit above is why — no deployed contract's ABI ever advertised `nonpayable`, so no consumer reading a handler is affected; only a client generated from the standalone `IFeeHandler` artifact would hold a write binding, and even that still works, at the cost of a needless transaction round-trip. Stacked on [#114](https://github.com/BitChillRSK/dca-contracts/pull/114).
- Assigned: [R62-idle-erc20-handler-dex.md](./R62-idle-erc20-handler-dex.md) (GitHub [#116](https://github.com/BitChillRSK/dca-contracts/pull/116)). Closes the idle×Uniswap hole: `IdleErc20HandlerDex` for `(USDRIF, 0)` and `(USDT0, 0)`; DOC stays `IdleDocHandlerMoc` at `(DOC, 0)` (never swapped on Uniswap). Runtime **12,940 B**, EIP-170 margin **11,636 B**. Stacked on [#115](https://github.com/BitChillRSK/dca-contracts/pull/115). Product decisions answered 2026-09-04. Consumer follow-ups: [front-end#15](https://github.com/BitChillRSK/front-end/issues/15), [front-end#20](https://github.com/BitChillRSK/front-end/issues/20), [swapper-bot#5](https://github.com/BitChillRSK/swapper-bot/issues/5), [data-api#9](https://github.com/BitChillRSK/data-api/issues/9), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10), [metrics-dashboard#7](https://github.com/BitChillRSK/metrics-dashboard/issues/7).
- Assigned: [R63-section-header-and-ordering-convention.md](./R63-section-header-and-ordering-convention.md) (GitHub [#117](https://github.com/BitChillRSK/dca-contracts/pull/117)). One banner vocabulary and function order in `AGENTS.md`, applied across first-party `src/`: `EXTERNAL FUNCTIONS` / `GETTERS` (not `FUNCTIONS` / `GETTER FUNCTIONS`); constructor-only leaves stay banner-free; `_batchBuyRbtc` under `PRIVATE FUNCTIONS`; `LendingErc20Handler` regrouped (mutators then views). Metadata-stripped runtime unchanged on all ten deployable contracts. Stacked on [#116](https://github.com/BitChillRSK/dca-contracts/pull/116).
- Assigned: [R64-batch-calldata-and-schedule-keying.md](./R64-batch-calldata-and-schedule-keying.md) (GitHub [#118](https://github.com/BitChillRSK/dca-contracts/pull/118)). Measurement PR; no `src/` behavior change. The premise R64 was opened to test is confirmed wrong, and the correction runs the other way from the one the spec expected. Passing schedule fields in calldata saves no `SLOAD` — both designs read exactly three cold slots per row, the nested one taking the array length plus two struct slots and the flat one taking three struct slots. But calldata is only **4.7%** of a batch's bill, so the flat key's win is mostly execution: of **1,984 gas per row** at 200 rows, 736 is calldata and 1,248 is decoding one array instead of four, one `keccak256` per row instead of three, and the index and id checks a flat key deletes. Three designs were benchmarked against one constant-cost stub handler under both profiles — today's nested storage (**18,981 gas/row**, via-IR 18,022), the same without the `purchaseAmounts` guard (18,569 / 17,485) and a flat `mapping(uint64 => …)` whose row is one id (16,997 / 16,090) — with the nested-storage prototype asserted to still match `DcaManager` on create and delete (within 264 gas, 0.3%) so a drift breaks the run instead of quietly measuring something else. **Both decisions: keep what is there.** The staleness guard costs **412 gas/row**, not the ~240 estimated, which is 2.2% of a row for a named revert instead of a purchase at an amount nobody quoted. The flat key is genuinely the better design on gas — 10.5% off every tick, break-even at **22.6 purchases** — and is still not worth taking: seven external signatures and five consumer repos change, `purchaseAmount` narrows to `uint96`, the struct grows from exactly two slots to three, create costs the user **48% more** (91,622 → 135,925), enumeration duplicates, and it is a storage redesign landing after the whole relaunch stack has been reviewed against the current one, for roughly $880 a year at 200 active daily schedules. **It cannot land after the relaunch deploy** — a flat mapping is not layout-compatible with a deployed `DcaManager` — so this is the last chance to overrule that. Two live-network findings fall out of the work and outlive it: **Rootstock does not price calldata the way Ethereum does** — measured with `eth_estimateGas` against mainnet rather than assumed, it is a 21,060 base plus EIP-2028's 4 and 16 gas per byte **plus 12 gas per 32-byte word**, so an Ethereum-schedule figure understates it by about 8% — and one tick fits about **525 rows** against a 10,000,000 block gas limit. Ships the `Batch` NatSpec correction regardless of the decision, saying the cross-check fields are a staleness guard and not a storage-read saving; comment-only, with metadata-stripped runtime byte-identical on all ten deployable contracts. Invariant 7 stands unchanged. No consumer issues: the only `src/` change is NatSpec. Stacked on [#117](https://github.com/BitChillRSK/dca-contracts/pull/117).
- Assigned: [R64-batch-calldata-and-schedule-keying.md](./R64-batch-calldata-and-schedule-keying.md) implementation (GitHub [#119](https://github.com/BitChillRSK/dca-contracts/pull/119)). The measurement PR recommended keeping the nested keying; the call went the other way, on the ground that the hot path is paid every day for years and a flat mapping is not layout-compatible with a deployed `DcaManager`, so this was the last moment to take it. **This cannot land after the relaunch deploy.** Shipped is #118's design C **with the `purchaseAmounts` staleness guard kept** (gate 1's answer), so it is not the C that was priced: the guard buys back 452 gas per row. A tick costs **1,470 gas per row less** under `[profile.default]` and **1,201** under the via-IR profile that ships — 7.8% and 6.8% — against **45,117 more on a create** (91,358 → 136,475, +49%) and ~1,000 more on a delete, a break-even of **31 to 39 purchases**, about six weeks for a daily schedule. Calldata for a 200-row batch halves, 25,988 → 13,060 bytes. `DcaManager`'s metadata-stripped runtime went **14,492 → 13,646 B**, 846 smaller, because the flat key deletes `validateScheduleIndex`, the id cross-check and the nested addressing; every deployable handler is byte-identical. `DcaSchedule` goes from exactly two slots to three, ordered so slot 0 still holds every field a purchase touches and a purchase is still one `SSTORE`; buying that costs `purchaseAmount` 32 bits (`uint128` → `uint96`, ~7.9e10 tokens at 18 decimals), because at `uint128` it no longer fits beside an address and the two writes split. `scheduleId` stays in the struct despite duplicating the key — it costs no slot, and without it `getDcaSchedules` would return schedules a caller could not address. **The hazard the change creates is that the mapping key used to make ownership unforgeable.** `s_dcaSchedules[msg.sender][token][index]` could not reach another account's schedule because the caller was the key; a flat key can reach anything, so ownership is now a stored field and an explicit check on every path, and one omission is another account's funds. `AGENTS.md` carries that as **invariant 8**, and `test/unit/ScheduleOwnershipTest.t.sol` walks the whole mutator surface rather than a sample: every mutator against a stranger's id, every mutator against an id belonging to nobody, id zero, and a check that a refused stranger left the schedule byte-for-byte as it was. The `onlySwapper` purchase path instead checks existence and that the schedule's token matches the batch's, so a row cannot have a schedule of another stablecoin debited by a handler that never holds its funds. ABI: eight external signatures drop `(token, scheduleIndex)`, `getDcaSchedule` is by id, `Batch` loses `buyers` and `scheduleIndexes`, `DcaSchedule` gains `user` and `token`, two errors are removed and three added, four dropped their `scheduleIndex` field — and **no event signature, field meaning, or indexed field moved**. Invariants 6 and 7 hold unchanged. Green on `make check`, `make check-deploy`, `make fork-sovryn` and `make fork-tropykus`. Consumer follow-ups: [front-end#24](https://github.com/BitChillRSK/front-end/issues/24), [swapper-bot#7](https://github.com/BitChillRSK/swapper-bot/issues/7), [data-api#9](https://github.com/BitChillRSK/data-api/issues/9#issuecomment-5542276174), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5542276507), [metrics-dashboard#7](https://github.com/BitChillRSK/metrics-dashboard/issues/7#issuecomment-5542280275) — each says the change is proposed and not yet merged. Stacked on [#118](https://github.com/BitChillRSK/dca-contracts/pull/118).
- Opened 2026-09-04 from review of R61's own diff: [R65-contract-header-natspec-ownership.md](./R65-contract-header-natspec-ownership.md), pre-cutover, editorial. Solidity inherits NatSpec for functions, events, errors and public state variables, so `@inheritdoc` already makes the function layer live once, on the interface. It does **not** inherit the header block above a contract, and that has a measured consequence: `LendingErc20Handler`'s devdoc contract-level `details` is empty even though `ITokenLending` carries a paragraph, while its `methods` carry twelve inherited `details` entries — whatever an implementation does not say itself is absent from the artifact that ships. With no mechanism to lean on, the twelve interface/implementation pairs in `src/` have landed on three different answers: paragraph on the implementation only (`PurchaseUniswap`, `IdleErc20Handler`, `LayerBankErc20Handler`), on the interface only (`LendingErc20Handler`, `TokenLending`), and on both (`OperationsAdmin`, whose 657-character contract `@dev` contains every claim of `IOperationsAdmin`'s 171-character one, the second copy without the reasons). `@notice` similarity across the pairs runs 0.28 to 0.94. The one-line `@notice` on each side is a label, not duplication, and stays. R61 fixed only the pair it created (`DcaManager`/`IDcaManager`, 1.00 on both tags after the custody rewrite); deciding the other eleven per file is what R65 exists to stop.
- Next unassigned: `Start with R65`. It is not from the ordered queue, which R60 closed. What remains besides them is the human-operator Rootstock deploy + Blockscout verification step above, the human licensing decision below, and review and merge of the open stack in order.
- Closed without implementation: owner sweep and handler balance/share narrowing; R13 cooperative migration and R50's merged per-user slot stay decided against (bytecode reasons void, non-size reasons stand — closed in [#111](https://github.com/BitChillRSK/dca-contracts/pull/111)). The final reasons live in [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md). R12 compounding is no longer among them — it was reopened as [R54](./R54-schedule-top-up-from-interest.md) and ships in [#110](https://github.com/BitChillRSK/dca-contracts/pull/110).
- Reopened 2026-08-31: **licensing**. MIT may not be the right license for BitChill, and `src/` declares MIT over GPL-2.0-or-later Uniswap imports today. Human decision, likely with counsel; free until cutover. See **Licensing — reopened** in [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md).
