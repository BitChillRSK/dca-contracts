# Relaunch task specs

Committed work orders for BitChill contract PRs. Implement from a spec here. The human prompt is `Start with R<n>` (see `AGENTS.md`).

## Progressive disclosure

1. Root [`AGENTS.md`](../../AGENTS.md).
2. [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md) — PR map and which product gates to ask.
3. **One** assigned spec in this folder (write it from [`TASK_TEMPLATE.md`](./TASK_TEMPLATE.md) if missing).
4. The files it names, then only imports / inheritance / mocks / failing tests / compiler errors.

Document any extra files in the PR. Do not search `out/`, `cache/`, or `lib/`. Do not implement from gitignored `.cursor/relaunch-plan.md` (read it only to draft the missing spec).

## How to add a spec

1. Copy [`TASK_TEMPLATE.md`](./TASK_TEMPLATE.md) to `R<n>-<short-slug>.md`.
2. Fill **Open product decisions** (or write **none**). Ask the human only if that section is not none and not yet answered.
3. Every remaining relaunch item is ordered in `IMPLEMENTATION_ORDER.md`; there is no optional-late queue.
4. One spec = one PR unless **Scope** names a tight bundle.

## Decision records

- [`EXTERNAL_REWARDS.md`](./EXTERNAL_REWARDS.md) (GitHub [#57](https://github.com/BitChillRSK/dca-contracts/pull/57)) — lending handlers distribute native protocol interest only; external campaigns use off-chain forwarding if available. R9 must add a canonical per-user lending-share balance-transition event so forwarding does not depend on transaction traces or a provider response.

## Status

- Merged: [R23-toolchain-baseline.md](./R23-toolchain-baseline.md) (PR 1, GitHub [#42](https://github.com/BitChillRSK/dca-contracts/pull/42)). Rootstock testnet accepted solc 0.8.36 / `cancun`.
- Assigned: [R2-utc-purchase-period.md](./R2-utc-purchase-period.md) (PR 3, GitHub [#44](https://github.com/BitChillRSK/dca-contracts/pull/44)). No product gates. Does not wait on PR 2.
- Assigned: [R7-dca-manager-small-fixes.md](./R7-dca-manager-small-fixes.md) (PR 4, GitHub [#45](https://github.com/BitChillRSK/dca-contracts/pull/45)). Bundles R7 / R11 / R14. No product gates. Stacked on [#44](https://github.com/BitChillRSK/dca-contracts/pull/44).
- Assigned: [R3-fee-handling.md](./R3-fee-handling.md) (PR 5, GitHub [#46](https://github.com/BitChillRSK/dca-contracts/pull/46)). Bundles R3 / R4 / R5. Keep linear (gate answered this chat). Stacked on [#45](https://github.com/BitChillRSK/dca-contracts/pull/45).
- Assigned: [R6-hot-path-cleanup.md](./R6-hot-path-cleanup.md) (PR 6, GitHub [#47](https://github.com/BitChillRSK/dca-contracts/pull/47)). Bundles R6 / R17. Spec revised after review and re-implemented. No product gates. Stacked on [#46](https://github.com/BitChillRSK/dca-contracts/pull/46).
- Assigned: [R8-remove-stuck-rbtc-rescue.md](./R8-remove-stuck-rbtc-rescue.md) (PR 7, GitHub [#48](https://github.com/BitChillRSK/dca-contracts/pull/48)). No product gates. Stacked on [#47](https://github.com/BitChillRSK/dca-contracts/pull/47).
- Assigned: [R1-integration-cash-accounting.md](./R1-integration-cash-accounting.md) (PR 8, GitHub [#49](https://github.com/BitChillRSK/dca-contracts/pull/49)). Bundles R1 / R20 and owns the R6 clamp-desync leftover. No product gates. Stacked on [#48](https://github.com/BitChillRSK/dca-contracts/pull/48).
- Assigned: [R24-test-harness-matrix.md](./R24-test-harness-matrix.md) (PR 9, GitHub [#50](https://github.com/BitChillRSK/dca-contracts/pull/50)). Test/Makefile only. Stacked on [#49](https://github.com/BitChillRSK/dca-contracts/pull/49). Merge after #49.
- Assigned: [R15-withdraw-all-sentinel.md](./R15-withdraw-all-sentinel.md) (PR 10, GitHub [#51](https://github.com/BitChillRSK/dca-contracts/pull/51)). Withdraw-all sentinel only; lending-share dust deferred (decision recorded in the spec). No product gates. Stacked on [#50](https://github.com/BitChillRSK/dca-contracts/pull/50).
- Assigned: [R22-repo-layout.md](./R22-repo-layout.md) (PR 11, GitHub [#52](https://github.com/BitChillRSK/dca-contracts/pull/52)). Folders only; no product gates. Stacked on [#51](https://github.com/BitChillRSK/dca-contracts/pull/51).
- Assigned: [R22-idle-handler.md](./R22-idle-handler.md) (PR 12, GitHub [#53](https://github.com/BitChillRSK/dca-contracts/pull/53)). Idle DOC + MoC handler at index 0. No product gates. Stacked on [#52](https://github.com/BitChillRSK/dca-contracts/pull/52).
- Assigned: [R21-fee-on-transfer-deposits.md](./R21-fee-on-transfer-deposits.md) (PR 13, GitHub [#54](https://github.com/BitChillRSK/dca-contracts/pull/54)). Hop-1 received-on-deposit. FOT unsupported; withdraw still works if a listed token turns on a fee. No product gates. Stacked on [#53](https://github.com/BitChillRSK/dca-contracts/pull/53).
- Assigned: [R16-redeem-glossary.md](./R16-redeem-glossary.md) (PR 14, GitHub [#55](https://github.com/BitChillRSK/dca-contracts/pull/55)). Rename-only; first-party "redeem" now names the asset given up. Keeps PR 13's 1-wei batch-redemption event check. No product gates. Stacked on [#54](https://github.com/BitChillRSK/dca-contracts/pull/54).
- Supporting: [EXTERNAL_REWARDS.md](./EXTERNAL_REWARDS.md) (GitHub [#57](https://github.com/BitChillRSK/dca-contracts/pull/57)). Docs-only external-incentive boundary and indexer-ready R9 share-event requirement. Stacked on the live Sovryn probe [#56](https://github.com/BitChillRSK/dca-contracts/pull/56); no Solidity or ABI change in this PR.
- Assigned: [R22-layerbank-handler.md](./R22-layerbank-handler.md) (PR 15, GitHub [#58](https://github.com/BitChillRSK/dca-contracts/pull/58)). LayerBank DOC + MoC handler at index 1. No product gates. Stacked on [#57](https://github.com/BitChillRSK/dca-contracts/pull/57).
- Assigned: [R25-lending-redeem-naming.md](./R25-lending-redeem-naming.md) (PR 16, GitHub [#59](https://github.com/BitChillRSK/dca-contracts/pull/59)). Rename-only: `_redeemByUnderlying` / `_redeemByShares`, `*ToRedeem` locals, Sovryn interest-local alignment, and the unused `minPurchaseAmount` constructor arg dropped from the Tropykus/Sovryn leaves. No product gates. Stacked on [#58](https://github.com/BitChillRSK/dca-contracts/pull/58).
- Planning: GitHub [#60](https://github.com/BitChillRSK/dca-contracts/pull/60). Specs only: R26, R27, and R28 (later promoted to PR 19). Stacked on [#59](https://github.com/BitChillRSK/dca-contracts/pull/59). Later reordered so R27/R28 precede R22 deploy/CI — see [#62](https://github.com/BitChillRSK/dca-contracts/pull/62).
- Assigned: [R26-share-terminology.md](./R26-share-terminology.md) (PR 17, GitHub [#61](https://github.com/BitChillRSK/dca-contracts/pull/61)). Rename-only: “lending token” → `shares` (`getUserShares`, `_stablecoinToShares` / `_sharesToStablecoin`, `TokenLending__SharesRedeemed(Batch)`, `TokenLending__InsufficientShares`, Sovryn `_redeemShares`). Preserves R16 `underlyingAmount` event params. No product gates. Stacked on [#60](https://github.com/BitChillRSK/dca-contracts/pull/60).
- Assigned: [R27-tropykus-lending-guards.md](./R27-tropykus-lending-guards.md) (PR 18, GitHub [#62](https://github.com/BitChillRSK/dca-contracts/pull/62)). Align Tropykus deposit/batch zero-cash guards with Sovryn/LayerBank. Reordered ahead of R22 deploy/CI. Stacked on [#61](https://github.com/BitChillRSK/dca-contracts/pull/61).
- Assigned: [R28-lending-erc20-handler.md](./R28-lending-erc20-handler.md) (PR 19, GitHub [#63](https://github.com/BitChillRSK/dca-contracts/pull/63)). Extract `LendingErc20Handler` (Idle out). Stacked on [#62](https://github.com/BitChillRSK/dca-contracts/pull/62).
- Assigned: [R29-hardcode-exchange-rate-scale.md](./R29-hardcode-exchange-rate-scale.md) (PR 20, GitHub [#64](https://github.com/BitChillRSK/dca-contracts/pull/64)). Hardcode Sovryn/Tropykus `EXCHANGE_RATE_DECIMALS = 1e18`; LayerBank renamed `RAY` → `EXCHANGE_RATE_DECIMALS` (`1e27`). Stacked on [#63](https://github.com/BitChillRSK/dca-contracts/pull/63).
- Assigned: [R30-purchase-pipeline.md](./R30-purchase-pipeline.md) (PR 21, GitHub [#65](https://github.com/BitChillRSK/dca-contracts/pull/65)). Combines review Candidates A+B: one shared purchase algorithm and one shared stablecoin-source seam; no ABI/product change. Stacked on [#64](https://github.com/BitChillRSK/dca-contracts/pull/64).
- Planning: GitHub [#66](https://github.com/BitChillRSK/dca-contracts/pull/66). Specs only: the former Candidates C–F are now assigned as R13 and R31–R34, with implementation order and product gates recorded. Stacked on [#65](https://github.com/BitChillRSK/dca-contracts/pull/65). R22 deploy/CI moves to PR 28, R9 to PR 29, and R10 to PR 30.
- Assigned: [R33-uniswap-slippage-validation.md](./R33-uniswap-slippage-validation.md) (PR 23, GitHub [#67](https://github.com/BitChillRSK/dca-contracts/pull/67)). One constructor/setter slippage invariant. No product gate. Stacked on [#66](https://github.com/BitChillRSK/dca-contracts/pull/66).
- Assigned: [R13-operations-admin-lifecycle.md](./R13-operations-admin-lifecycle.md) (PR 24, GitHub [#68](https://github.com/BitChillRSK/dca-contracts/pull/68)). One owner-governance boundary, a typed form of the existing multi-swapper allowlist, an explicit one-shot route-class registry, and add-only versioned handler routes. Migration gate answered as option (a), manual exit/re-entry; idle is a route class rather than index `0`. Stacked on [#67](https://github.com/BitChillRSK/dca-contracts/pull/67).
- Assigned: [R31-handler-abi-trim.md](./R31-handler-abi-trim.md) (PR 25, GitHub [#69](https://github.com/BitChillRSK/dca-contracts/pull/69)). Remove redundant handler selectors and dead abstract-base token parameters before R9; fee-band mutation is atomic-only. Closes R13's class↔handler ERC-165 hole (`ITokenLending` on `assignTokenHandler`). Stacked on [#68](https://github.com/BitChillRSK/dca-contracts/pull/68).
- Assigned: [R34-dca-manager-abi.md](./R34-dca-manager-abi.md) (PR 26, GitHub [#70](https://github.com/BitChillRSK/dca-contracts/pull/70)). Canonical schedule reads and routing; mutation surface is intent-specific (`depositToken` / `setPurchaseAmount` / `setPurchasePeriod`), `updateDcaSchedule` removed. Stacked on [#69](https://github.com/BitChillRSK/dca-contracts/pull/69).
- Assigned: [R35-route-index-terminology.md](./R35-route-index-terminology.md) (PR 27, GitHub [#71](https://github.com/BitChillRSK/dca-contracts/pull/71)). Rename DcaManager `lendingProtocolIndex` → `routeIndex` after #70. Stacked on [#70](https://github.com/BitChillRSK/dca-contracts/pull/70).
- Assigned: [R32-internal-cleanup.md](./R32-internal-cleanup.md) (PR 28, GitHub [#72](https://github.com/BitChillRSK/dca-contracts/pull/72)). Behavior-preserving DcaManager/lending cleanup after the public surfaces settle. No product gate. Stacked on [#71](https://github.com/BitChillRSK/dca-contracts/pull/71).
- Assigned: [R22-deploy-ci.md](./R22-deploy-ci.md) (PR 29, GitHub [#73](https://github.com/BitChillRSK/dca-contracts/pull/73)). Production MoC map is idle / LayerBank / Sovryn; CI and `DeployMocSwaps` match. Stacked on [#72](https://github.com/BitChillRSK/dca-contracts/pull/72).
- Planning: GitHub [#74](https://github.com/BitChillRSK/dca-contracts/pull/74). Final specs and decisions: dead purchase selector, OZ5/ownership/authority hardening, both pause controls, schedule packing, Dex policy/routes, withdraw API, batcher, event/ABI freeze, and final natspec. Stacked on [#73](https://github.com/BitChillRSK/dca-contracts/pull/73). Every remaining prompt is gate-free; there is no optional-late queue.
- Assigned: [R39-remove-single-buy.md](./R39-remove-single-buy.md) (PR 30, GitHub [#75](https://github.com/BitChillRSK/dca-contracts/pull/75)). `buyRbtc` deleted from `DcaManager`, `PurchaseRbtc`, and both interfaces, along with the now-unreachable `PurchaseRbtc__RbtcPurchaseFailed`; a length-1 `batchBuyRbtc` is the one-schedule path. Frees 551 bytes on `DcaManager` and ~660 on every purchase handler (Dex margin 1,743 → 2,403 and 1,487 → 2,147). Measured one-schedule premium ~13–15k gas, accepted. No product gates. Stacked on [#74](https://github.com/BitChillRSK/dca-contracts/pull/74).
- Assigned: [R44-openzeppelin-5-upgrade.md](./R44-openzeppelin-5-upgrade.md) (PR 31, GitHub [#76](https://github.com/BitChillRSK/dca-contracts/pull/76)). OpenZeppelin pinned to tag `v5.7.0`: `ReentrancyGuard` moved to `utils/`, `Ownable(msg.sender)`, `safeApprove` → `forceApprove`, `Math.Rounding.Up` → `Ceil`, and OZ revert strings → custom errors (assertions kept exact via `test/utils/OzRevert.sol`). No first-party selector or event change. Every contract shrank — Dex margin 2,403 → 3,916 and 2,147 → 3,660 — and no production path got more expensive. OZ 5.x holds the reentrancy word in an ERC-7201 namespaced slot, so DcaManager's BitChill variables shift down one slot with no relative reordering; handlers are unchanged. No product gates. Stacked on [#75](https://github.com/BitChillRSK/dca-contracts/pull/75).
- Assigned: [R45-two-step-ownership.md](./R45-two-step-ownership.md) (PR 32, GitHub [#82](https://github.com/BitChillRSK/dca-contracts/pull/82)). Shared `BitChillOwnable` over `Ownable2Step`: Foundry broadcasts from an EOA; testnet that EOA is owner, mainnet proposes the Safe which must `acceptOwnership`. Renunciation is impossible. Dex constructors no longer call `onlyOwner` during init. Handlers grew ~387 bytes; Dex margin 3,916 → 3,515 and 3,660 → 3,259. No product gates. Stacked on [#76](https://github.com/BitChillRSK/dca-contracts/pull/76).
- Assigned: [R46-pin-operations-admin.md](./R46-pin-operations-admin.md) (PR 33, GitHub [#83](https://github.com/BitChillRSK/dca-contracts/pull/83)). `DcaManager` pins its constructor `OperationsAdmin` as immutable; `setOperationsAdmin` and `DcaManager__OperationsAdminUpdated` are gone. Runtime −187 bytes; the old `s_operationsAdmin` slot is removed. No product gates. Stacked on [#82](https://github.com/BitChillRSK/dca-contracts/pull/82).
- Out of band, merge first: CI toolchain pin, GitHub [#78](https://github.com/BitChillRSK/dca-contracts/pull/78). Branched off `main`, **not** stacked, so it lands ahead of this stack; rebase the stack onto it. `foundry-toolchain` and the forge version were both moving refs, and `stable` rolled to forge 1.8.0 on 2026-08-26, failing 10 `setUp()`s repo-wide. Reproduced on an unmodified pre-R44 commit, so it is the toolchain, not the stack.
- Not a relaunch item, tracked as GitHub issue [#79](https://github.com/BitChillRSK/dca-contracts/issues/79): migrating the test harness to Foundry 1.8.0 so [#78](https://github.com/BitChillRSK/dca-contracts/pull/78)'s pin can be raised. Deliberately an issue and **not** a spec here — it may never be done, and this queue holds only ordered, committed relaunch work. The issue carries the reproduction and the two hypotheses already disproven.
- Assigned: [R47-handler-address-uniqueness.md](./R47-handler-address-uniqueness.md) (PR 34, GitHub [#84](https://github.com/BitChillRSK/dca-contracts/pull/84)). An `OperationsAdmin` assigns each handler address exactly once; reuse reverts `OperationsAdmin__HandlerAddressAlreadyInUse` whatever the token or route class. Closes the cross-route hole where DcaManager locks principal per route while handler shares are per handler. `OperationsAdmin` runtime +225 bytes (4,625 → 4,850, margin 19,726); one appended mapping at slot 5, slots 0–4 untouched. No product gates. Stacked on [#83](https://github.com/BitChillRSK/dca-contracts/pull/83).
- Assigned: [R43-dex-path-review.md](./R43-dex-path-review.md) (PR 35, GitHub [#85](https://github.com/BitChillRSK/dca-contracts/pull/85)). Uniswap `amountOutMinimum` is scaled by the stablecoin's `decimals()`, so USDT0's 6-decimal floor is no longer 1e12 too small; more than 18 decimals reverts at deploy. The $1 peg, the execution-time oracle check, the absent router deadline, and the config-only safety check are recorded rather than changed. R39's two handoff questions are answered keep-as-is (batch still reverts on a share shortfall; fee still charged on the planned gross). Dex runtime +43 bytes (margins 3,472 and 3,216); one new deploy-time custom error. No product gates. Stacked on [#84](https://github.com/BitChillRSK/dca-contracts/pull/84).
- Assigned: [R41-reject-fot-deposits.md](./R41-reject-fot-deposits.md) (PR 36, GitHub [#86](https://github.com/BitChillRSK/dca-contracts/pull/86)). R21's hop-1 measurement stays, but the predicate tightens from `received == 0` to `received != requested`: a deposit now credits the full request or reverts `TokenHandler__DepositAmountMismatch(requested, received)` with a full rollback. `TokenHandler__ZeroStablecoinReceived` is gone (zero-received is that error with `received == 0`); the redeem-side `TokenLending__ZeroStablecoinReceived` is a different error and stays. Hop-2 lag coverage moved to a 1:1 stablecoin plus `MockKdocToken.setMintShortfallBps`. Handlers +1 byte; Dex margins 3,471 and 3,215. No product gates. Stacked on [#85](https://github.com/BitChillRSK/dca-contracts/pull/85).
- Assigned: [R40-update-purchase-period.md](./R40-update-purchase-period.md) (PR 37, GitHub [#87](https://github.com/BitChillRSK/dca-contracts/pull/87)). `setPurchaseAmount` / `setPurchasePeriod` become `updatePurchaseAmount` / `updatePurchasePeriod`, each emitting the value it replaced next to the new one (`PurchaseAmountUpdated`, `PurchasePeriodUpdated`, neither value indexed, so R9 inherits the frozen shape). The spec covered only the period setter; extended to both on 2026-08-28 because the two are symmetric and splitting them would break the ABI twice. Combined amount+period edits are still two transactions. `DcaManager` runtime +81 bytes (16,483 → 16,564, margin 8,012). No product gates. Stacked on [#86](https://github.com/BitChillRSK/dca-contracts/pull/86).
- Assigned: [R48-deposit-pause.md](./R48-deposit-pause.md) (PR 38, GitHub [#88](https://github.com/BitChillRSK/dca-contracts/pull/88)). Owner-only `setDepositsPaused(token, routeIndex, paused)` on `OperationsAdmin`: an assigned pair only, a real transition only, so every `OperationsAdmin__DepositsPauseSet` is a genuine state change. `DcaManager` consults it through one private `_handlerForDeposit` used by `depositToken` and `createDcaSchedule` alone, before any transfer; every other caller keeps `_handler`, so purchases, edits, deletion, and all three withdrawal paths ignore the pause by construction. `OperationsAdmin` runtime +921 bytes (4,850 → 5,771, margin 18,805); `DcaManager` +246 (16,564 → 16,810, margin 7,766); one mapping appended at slot 6, slots 0–5 untouched. No multi-pair form: a token-wide sweep is one transaction per pair and must skip pairs already paused, recorded in the spec's ops runbook note. No product gates. Stacked on [#87](https://github.com/BitChillRSK/dca-contracts/pull/87).
- Assigned: [R19-schedule-pause.md](./R19-schedule-pause.md) (PR 39, GitHub [#89](https://github.com/BitChillRSK/dca-contracts/pull/89)). User-owned `setSchedulePaused(token, scheduleIndex, scheduleId, paused)` keyed off `msg.sender`, so it reaches only the caller's own schedules. `DcaDetails` gains `bool paused`, appended after `routeIndex` for R18 to place. Only the purchase path reads it: `_rBtcPurchaseChecksEffects` reverts `DcaManager__SchedulePaused(user, token, scheduleId, scheduleIndex)` after id validation and before the balance debit and timestamp write, so one paused row fails the whole `batchBuyRbtc` with no sibling row keeping a debit or a consumed period. Deposits, edits, all three withdrawal paths, interest and rBTC claims, and deletion ignore the flag by construction — a pause can never strand funds. Unlike R48, a repeated write is a silent no-op rather than a revert: the caller is a user re-sending a click, not a Safe, and the early return gives the same "every event is a real transition" property. The flag lives in the struct, so swap-pop carries it with its schedule. `DcaManager` runtime +1,043 bytes (16,810 → 17,853, margin 6,723). The unpacked bool also costs the **protocol-paid** purchase path a seventh cold slot per row — `testSinglePurchase` 304,203 → 307,788 (+3,585), `testBatchPurchasesOneUser` +76,492 — which is why **R18 must follow immediately**: it packs `paused` in with `purchasePeriod`/`lastPurchaseTimestamp`/`routeIndex` and removes that read. Pause also makes the pre-existing cross-user batch DoS cheaper (an amount edit already reverts a batch identically, but a pause is free and reversible); mitigation is the bot's off-chain filter, and R42 bundling would widen it to every venue in a bundle. No product gates. Stacked on [#88](https://github.com/BitChillRSK/dca-contracts/pull/88).
- Assigned: [R49-schedule-struct-name.md](./R49-schedule-struct-name.md) (PR 40, GitHub [#90](https://github.com/BitChillRSK/dca-contracts/pull/90)). Rename-only `DcaDetails` → `DcaSchedule`: `Details` is a noise word, and every identifier around the struct already says "schedule", including the locals that hold it. Same correction class as R26 and R35. `DcaSettings` rejected — the struct carries protocol-written `tokenBalance` and `lastPurchaseTimestamp`, so naming it after its config half invites R6's stale-write-back hazard. Verified rather than asserted: `forge inspect methodIdentifiers` byte-identical, `storageLayout` identical slots with only the type label moving, and the full suite passes with no test edit beyond the type name. Deliberately before R18 so packing is written against the final name, and well before R9's freeze. No product gates. Stacked on [#89](https://github.com/BitChillRSK/dca-contracts/pull/89).
- Assigned: [R18-storage-packing.md](./R18-storage-packing.md) (PR 41, GitHub [#91](https://github.com/BitChillRSK/dca-contracts/pull/91)). `DcaSchedule` is three slots: two `uint128` amounts; `uint32` period + `uint48` timestamp + `uint32` route + `bool paused`; `bytes32 scheduleId`. External inputs stay `uint256`; overflow is OZ `SafeCast` before cash moves. `testSinglePurchase` 307,788 → 285,208; five-row batch −217k gas. Handler share mappings stay `uint256`. No product gates. Stacked on [#90](https://github.com/BitChillRSK/dca-contracts/pull/90).
- Assigned: [R36-layerbank-usdrif-dex.md](./R36-layerbank-usdrif-dex.md) (PR 42, GitHub [#92](https://github.com/BitChillRSK/dca-contracts/pull/92)). `LayerBankErc20HandlerDex` deployed twice at dex index 1: USDRIF (replacing Tropykus) and USDT0 (new, 6-decimal min/fee). Runtime 21,578 bytes (EIP-170 margin 2,998). kUSDRIF mint is paused at the tip (`C2`). Stacked on [#91](https://github.com/BitChillRSK/dca-contracts/pull/91).
- Planning: [R50-packing-follow-up.md](./R50-packing-follow-up.md) (GitHub [#93](https://github.com/BitChillRSK/dca-contracts/pull/93)). Spec only: R50 inserted as PR 43. Stacked on [#92](https://github.com/BitChillRSK/dca-contracts/pull/92).
- Assigned: [R50-packing-follow-up.md](./R50-packing-follow-up.md) (PR 43, GitHub [#94](https://github.com/BitChillRSK/dca-contracts/pull/94)). `DcaSchedule` is two slots: the `keccak256(user, token, nonce)` id becomes the `uint64` nonce itself, and the struct is ordered by what a purchase *writes* — `tokenBalance` + `lastPurchaseTimestamp` + `paused` in slot 0, so the purchase's second write is a ~100-gas dirty write instead of a clean write to a second slot; the read-only half fills slot 1 exactly and `scheduleId` stays last. Ids start at 1 and `getSchedulesCreatedCount()` is the last one assigned; an exhausted counter reverts `SafeCastOverflowedUintDowncast` before the deposit is pulled. Fee settings are two slots (collector + both `uint16` rates, then two `uint128` bounds — the collector is declared first so the rates do not fall into the free bytes beside `_pendingOwner` and push it out), `OperationsAdmin` holds handler and deposit pause in one `TokenRoute` value, DcaManager's scalars share one slot with the nonce, and the two Uniswap percents share another. Every OperationsAdmin route-index argument is `toUint32()`-bounded, including `setDepositsPaused` and the getters — so a malformed oversized index in `withdrawAllAccumulated*` now reverts instead of being skipped. Handler per-user share/balance and rBTC mappings stay `uint256` and stay separate: merging them into one packed slot was tried and reverted, because the only shared parent is `StablecoinSource`, the funding-hook seam, which should not own rBTC state — the reasoning is recorded in the spec's **Out of scope** so it is not re-attempted as a drive-by. Create −30,306 gas, single purchase −9,012, five-row batch −77,936. Runtime grows (mask/shift on packed fields): `DcaManager` 22,518 (margin 2,058), `LayerBankErc20HandlerDex` 23,032 (margin 1,544). Those are deploy sizes, not just a CI ceiling — every documented `--broadcast` command runs under `[profile.default]`, which is `via_ir = false`. No product gates. Stacked on [#93](https://github.com/BitChillRSK/dca-contracts/pull/93).
- Out of band, stacked on [#94](https://github.com/BitChillRSK/dca-contracts/pull/94): GitHub [#95](https://github.com/BitChillRSK/dca-contracts/pull/95). Not a relaunch item. Nit of [#85](https://github.com/BitChillRSK/dca-contracts/pull/85): one `ORACLE_DECIMALS` constant, no WRBTC `decimals()` check; and `depositToken` no longer returns the amount it already guaranteed.
- Assigned: [R37-retire-tropykus-live-paths.md](./R37-retire-tropykus-live-paths.md) (PR 44, GitHub [#96](https://github.com/BitChillRSK/dca-contracts/pull/96)). `TROPYKUS_INDEX` lives in `test/Constants.sol`, which no `script/` file imports, so a deploy script naming a Tropykus route fails to compile rather than failing review; `TROPYKUS_STRING` stays in `script/Constants.sol` because the helper configs select mocks with it. `DeployDexSwaps`' live branch drops the Tropykus `registerRoute` / deploy / assign and reverts on `Protocol.TROPYKUS`, matching `DeployMocSwaps` — the live dex map is LayerBank (USDRIF / USDT0) plus Sovryn (DOC), and index 4 stays burned. No `src/` change and no ABI change. `script/tropykus-legacy/` was **not** created: R36 repurposed `DeployUsdrifHandler` wholesale, so no Tropykus-only script remains to move and every other script is shared with a live path — compile scope carries the `src/` ↔ `script/` split instead. `UsdrifHelperConfig`'s write-only `kUsdrifTokenAddress` is removed as R36's comment promised. Passing counts are identical in all six measured lanes; each gains one test and one skip from `test_dexLive_revertsForTropykus`, and `dex-tropykus` trades the live-map test for it. No product gates. Stacked on [#95](https://github.com/BitChillRSK/dca-contracts/pull/95).
- Assigned: [R38-withdraw-all-route-pairs.md](./R38-withdraw-all-route-pairs.md) (PR 45, GitHub [#97](https://github.com/BitChillRSK/dca-contracts/pull/97)). `withdrawAllAccumulatedInterest` and `withdrawAllAccumulatedRbtc` take positional `(token, routeIndex)` pairs instead of a `tokens × routeIndexes` grid, so a user holding two tokens at two different routes stops calling the handlers on the combinations they do not hold. Same selectors, changed argument meaning — deliberate, with no legacy alias, which R34's no-dual-window cutover makes acceptable. Empty input now reverts `DcaManager__EmptyWithdrawalArrays` instead of succeeding silently, mismatched lengths revert `DcaManager__ArraysLengthMismatch` (the same error `batchBuyRbtc` uses), and every existing skip (unassigned handler, non-lending route, zero rBTC balance) is unchanged, so over-listing pairs is still safe. `isLendingRoute` runs once per pair rather than once per combination. The mixed case is proven by call counts on a *registered* cross-pair handler — an unassigned one would be skipped on the zero-address check and prove nothing — and that test fails against the old nested loop. `DcaManager` runtime 22,470 → 22,578 bytes (margin 2,106 → 1,998); no handler changed. Frontend follow-up: [front-end#21](https://github.com/BitChillRSK/front-end/issues/21) ([#13](https://github.com/BitChillRSK/front-end/issues/13) covered the same ground and is closed). No product gates. Stacked on [#96](https://github.com/BitChillRSK/dca-contracts/pull/96).
- Assigned: [R42-swapper-batcher.md](./R42-swapper-batcher.md) (PR 46, GitHub [#98](https://github.com/BitChillRSK/dca-contracts/pull/98)). `SwapperBatcher.batchBuyRbtcGroups` forwards several `DcaManager.batchBuyRbtc` groups in one transaction without adding `multicall` to the manager or taking custody of user funds. Callers and the batcher itself authenticate against the same OperationsAdmin swapper list (without the outer check, a random EOA could consume one due schedule and revert the bot's atomic bundle). Failures stay all-or-nothing, re-confirmed against R19. The bot EOA stays allowlisted for per-handler retries. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6). No product gates. Stacked on [#97](https://github.com/BitChillRSK/dca-contracts/pull/97).
- Assigned: [R9-event-indexing.md](./R9-event-indexing.md) (PR 47, GitHub [#99](https://github.com/BitChillRSK/dca-contracts/pull/99)). Event/indexing/share/fee audit and ABI freeze. Every existing scalar address and `scheduleId` is indexed, and nothing else is; dynamic address arrays and encoded paths stay in data. `DcaScheduleDeleted` now indexes user, token, and scheduleId; `PurchaseUniswap_OracleUpdated` indexes both oracle addresses. Lending handlers emit `TokenLending__UserSharesUpdated` after every virtual share mint or burn; purchase fees emit `FeeHandler__FeeTransferred` when non-zero. `SchedulePauseSet` stays user+scheduleId (no token). Cutover: [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22). No product gates. Stacked on [#98](https://github.com/BitChillRSK/dca-contracts/pull/98).
- Assigned: [R10-natspec.md](./R10-natspec.md) (PR 48, GitHub [#100](https://github.com/BitChillRSK/dca-contracts/pull/100)). First-party natspec/comments pass after the ABI freeze. User-facing docs on interfaces; `@inheritdoc` in implementations. No behavior change. No product gates. Stacked on [#99](https://github.com/BitChillRSK/dca-contracts/pull/99).
- Assigned: [R42-swapper-batcher.md](./R42-swapper-batcher.md) integration follow-up (PR 49, GitHub [#101](https://github.com/BitChillRSK/dca-contracts/pull/101)). `DcaManager.batchBuyRbtcAcrossHandlers` replaces the standalone batcher, authenticates once, and purchases each handler's batch through the same one-handler helper. The retry entry is `batchBuyRbtc(Batch)`; the former six-argument selector does not ship. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22), and [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10). Stacked on [#100](https://github.com/BitChillRSK/dca-contracts/pull/100).
- Planning review: GitHub [#102](https://github.com/BitChillRSK/dca-contracts/pull/102) finalizes the R51/R52 specs and targets their implementation at GitHub #103 and #104. Stacked on [#101](https://github.com/BitChillRSK/dca-contracts/pull/101).
- Assigned: [R51-per-batch-min-rbtc-out.md](./R51-per-batch-min-rbtc-out.md) (PR 50, GitHub [#103](https://github.com/BitChillRSK/dca-contracts/pull/103)). Appends aggregate `minRbtcOut` to PR 101's existing `Batch` tuple and enforce it against measured output in the shared handler pipeline. Units are always WRBTC/native-rBTC wei; `0` preserves current behavior, while the production bot sends a fresh quote-derived minimum for Dex. PR 103 keeps the 99.5% / 95% source defaults, records one fork table and opens the off-chain issues, but does not wait for ongoing calibration. Before relaunch the Safe chooses the tightest durable per-handler oracle backstop that clears the measured envelope with quantified headroom, records its maximum-loss budget, and re-locks the safety check to the same value in a separate Safe execution. Weekly execution is automatic quote/retry/split/failover, and whichever of the static oracle floor or dynamic caller minimum is stricter wins; monitoring alerts on a zero Dex minimum while exempting MoC. Swapper-bot remains the sole signer and consumes reusable/read-only route simulation logic derived from `rsk-uniswap-pools`. Implemented as specified except for one corrected instruction: both DcaManager purchase selectors change (`0x8e78f242`, `0x6885b843`) and the handler's becomes `0x0a0c5350`, but the spec's mandated `_creditBuyers` extraction is **not** needed and was not shipped. The added parameter does overflow the no-IR stack, by exactly one slot, and scoping `aggregatedFee` to the block that pays the fee releases it — so the loop stays inline and keeps its cached `buyers.length`, measurably cheaper (−87 gas single purchase, −416 five-row batch) and 17 bytes smaller per Dex handler than extracting. The loop also holds its twice-read `netStablecoinAmountsToSpend[i]` / `buyers[i]` in locals, worth 74 gas per row and 42 bytes per handler — much the largest of the three effects; the block scope is −28 / −298, and the length cache costs ~12 gas per call to save 3.00 per row. Enabling the legacy optimizer does not relieve stack-too-deep (checked); only via-IR does, and that stays out of bounds for EIP-170 decisions. `DcaManager` +20 bytes (23,703, margin 873), each Dex handler +57 (LayerBank 23,475 / margin 1,101; Sovryn 23,015 / 1,561; Tropykus 23,271 / 1,305); `OperationsAdmin` unchanged. The gating fork table is `make probe-dex-quote-floor` at pinned block 9198813, and its findings are negative for two of three routes: Sovryn DOC is **starved** (the DOC/rUSDT 0.05% pool held ~1.48 rUSDT, so every size partially fills at ~0.19 DOC and splitting cannot help), LayerBank USDRIF misses 99.5% by 16 bps at the $25 minimum and 23 bps at $1,000, and only LayerBank USDT0 clears — by 40 and 34 bps at those sizes, failing $100,000 by 466 bps on price impact. Per the spec those negatives are valid PR evidence and do not block merge. The DOC result also settled a route question: **Sovryn DOC is not a shipped Dex route and must never become one** — DOC buys rBTC only through MoC redemption, and may appear in a Uniswap path only as an intermediate hop. The shipped Dex set is LayerBank USDRIF and LayerBank USDT0; this supersedes the live-map wording in the R37 (PR 44) entry above. The DOC Dex handler stays test-only legacy, as Tropykus did after R37. Not fixed here and tracked as [R57](./R57-close-doc-dex-deploy-hole.md): `DeployDexSwaps`' live branch can still construct that handler when `STABLECOIN_TYPE=DOC`, so the rule is documentation-only until the script enforces it. Also recorded, not changed: `DexHelperConfig` comments both hops as "rUSDT", but the USDRIF hop `0xAf368c…` is 6-decimal `USDT`, a different token from the 18-decimal `rUSDT` `0xef2134…` on the DOC path; re-approving a path is R52's surface. Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6#issuecomment-5477257073), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10#issuecomment-5477260356), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22#issuecomment-5477262287), and the quote engine [rsk-uniswap-pools#2](https://github.com/BitChillRSK/rsk-uniswap-pools/issues/2); `data-api` needs none (it does not encode `Batch`). No contract-PR product gates; calibration and one-time cutover approval gate Dex relaunch. Stacked on [#102](https://github.com/BitChillRSK/dca-contracts/pull/102).
- Assigned: [R52-allowlisted-dex-path-failover.md](./R52-allowlisted-dex-path-failover.md) (PR 51, GitHub [#104](https://github.com/BitChillRSK/dca-contracts/pull/104)). Handler-local exact-path allowlist on `PurchaseUniswap`; constructor self-allowlists the initial path (emits `NewPathSet` + `PurchasePathAllowedSet`); handler owner approves later paths; an OperationsAdmin swapper or that handler owner may activate only an allowed path. Purchases do not SLOAD the allowlist. Default profile is optimizer-on / no-IR (`optimizer = true`, `optimizer_runs = 200`, `via_ir = false`). Runtime (EIP-170 24,576): `DcaManager` 13,767 (margin 10,809); `OperationsAdmin` 3,227 (21,349); `LayerBankErc20HandlerDex` 15,592 (8,984); `SovrynErc20HandlerDex` 15,379 (9,197); `TropykusErc20HandlerDex` 15,523 (9,053). Cutover: [swapper-bot#6](https://github.com/BitChillRSK/swapper-bot/issues/6), [bitchill-monitoring#10](https://github.com/BitChillRSK/bitchill-monitoring/issues/10), [front-end#22](https://github.com/BitChillRSK/front-end/issues/22), [rsk-uniswap-pools#2](https://github.com/BitChillRSK/rsk-uniswap-pools/issues/2). Optimizer-on proof **passed** 2026-09-01: CREATE [0x3a63dc…](https://rootstock-testnet.blockscout.com/tx/0x3a63dc2458142cca09144a3f290ed6d996780c616acb8adbdf15f57f736ff5bc) verified [`OperationsAdmin` `0x8D7B64ed7Ef7B862bB52c7381b9246d2669a4FAD`](https://rootstock-testnet.blockscout.com/address/0x8D7B64ed7Ef7B862bB52c7381b9246d2669a4FAD). Ready for human review. R53's optimizer-baseline work is absorbed here; remaining R54 (schedule top-up) and R55 (solx / via-IR / `ZeroTokenPurchaseUniswap`) live in stacked [#105](https://github.com/BitChillRSK/dca-contracts/pull/105), which needs rebase after this PR. Stacked on [#103](https://github.com/BitChillRSK/dca-contracts/pull/103).
- Planning review: GitHub [#105](https://github.com/BitChillRSK/dca-contracts/pull/105) queues [R53](./R53-optimizer-baseline.md) / [R54](./R54-schedule-top-up-from-interest.md) / [R55](./R55-solx-and-ir-evaluation.md) / [R56](./R56-dex-oracle-floor-is-safety-check.md) and reopens licensing. Comment-only Foundry section banners on five `src/` bases; no Solidity behavior. Stacked on [#104](https://github.com/BitChillRSK/dca-contracts/pull/104).
- Assigned: [R57-close-doc-dex-deploy-hole.md](./R57-close-doc-dex-deploy-hole.md) (PR 52, GitHub [#106](https://github.com/BitChillRSK/dca-contracts/pull/106)). Live `DeployDexSwaps` reverts on DOC (`"DOC is not on the production dex map"`) before constructing a Dex handler, so an unset `STABLECOIN_TYPE` can no longer burn `(DOC, SOVRYN_INDEX)`. Sovryn stays registered as a lending route. Local/fork DOC Dex handlers stay for `make dex-sovryn` and `ComparePurchaseMethods`. No `src/` change. Stacked on [#105](https://github.com/BitChillRSK/dca-contracts/pull/105).
- Next unassigned: `Start with R56` — Dex oracle floor becomes the safety check ([R56-dex-oracle-floor-is-safety-check.md](./R56-dex-oracle-floor-is-safety-check.md)). Then R53 -> R54 -> R55.
- Queued: [R56-dex-oracle-floor-is-safety-check.md](./R56-dex-oracle-floor-is-safety-check.md) (unassigned, **gated on #103 and #104**). R51 follow-up that 103 correctly left out of scope. Swap-time Uniswap `amountOutMinimum` uses `s_amountOutMinimumSafetyCheck` (deploy default 95%) as the compromised-bot / stale-tx loss ceiling; `minRbtcOut` is the operational bound from the quote. Drop `s_amountOutMinimumPercent` from swap math and from the handler ABI. Router min is `max(oracleFloor, minRbtcOut)`; `PurchaseRbtc` still checks measured output. No contract-PR product gates.
- Queued: [R53-optimizer-baseline.md](./R53-optimizer-baseline.md) (unassigned, **residual after #104**). The optimizer flip itself is no longer R53's: R52/[#104](https://github.com/BitChillRSK/dca-contracts/pull/104) pins `optimizer = true` / `optimizer_runs = 200` / `via_ir = false` on `[profile.default]` because handler-local path policy needs it to clear EIP-170, and #104 also owns the Rootstock testnet + Blockscout re-proof of that artifact as its merge blocker. What remains here is the re-baseline #104 did not do: it corrected only its own figures, so every size, EIP-170 margin, and gas number recorded in the R18 / R31 / R42 / R50 / R51 entries and specs is still unoptimized. Append the corrected figure to each historical entry without rewriting why a shipped PR chose what it chose, and re-point any rule that still reads as "unoptimized no-IR is the measurement basis". For reference, the flip moved `DcaManager` 23,703 B / 873 B margin → 13,767 B / 10,809 B, `testSinglePurchase` 283,711 → 243,817 gas (−14.1%), and `testBatchPurchasesOneUser` 2,244,557 → 1,562,720 (−30.4%). It also re-opens decisions that were justified by bytecode scarcity that was an artifact of the measurement — the migration gate's manual exit/re-entry and R51's `_creditBuyers` reasoning — each re-judged in its own PR, not this one. `via_ir` stays off (R55 owns it).
- Queued: [R54-schedule-top-up-from-interest.md](./R54-schedule-top-up-from-interest.md) (unassigned, **gated on #104**). Revives R12. One `DcaManager`-only entry point credits accrued lending interest to a schedule's `tokenBalance` so yield buys rBTC without a withdraw-then-deposit round trip, which also avoids Sovryn's SIP-0094 exit fee. No handler call, no token movement, no redeem-then-remint: it raises the schedule's claim over funds already in the lending position. R12's recorded rejection ("in-handler compound path", "cash-moving entry point to immutable handlers") described a design that was never proposed; the real blocker was EIP-170, and it was real — measured at `proto/r12-compound-interest` (`7dd00fb`) the function is +1,020 B unoptimized (margin **−147**, does not fit), +600 B with the optimizer (margin 10,209). Three product gates: whether the R48 deposit pause blocks a top-up (recommend yes), naming (`topUpFromInterest`, since "compound" collides with Compound the protocol), and all-or-part (recommend all). Additive ABI; front-end, data-api, and monitoring cutovers.
- Queued: [R55-solx-and-ir-evaluation.md](./R55-solx-and-ir-evaluation.md) (unassigned, **gated on #104**). Measure-and-recommend only, changing no compiler setting. Decide between stock solc no-IR, stock solc `via_ir`, and solx on size, hot-path gas, full-matrix pass, and Blockscout verifiability on Rootstock. Every number must be against the optimized baseline #104 pins: the optimizer already takes 14–30% and `via_ir` a further 2–4%, so solx competes against ~239k gas on `testSinglePurchase`, not ~284k. EIP-170 is explicitly not a motivation — nothing is size-constrained once #104 lands. `via_ir` is not free today: a full `forge build` fails with solc error 1284 on `ZeroTokenPurchaseUniswap`, whose always-reverting constructor is the test itself, and `forge test --match-*` masks this by compiling sparsely. Deciding factor is miscompilation and verification risk on immutable contracts, not gas alone. No product gates — the adoption decision is this PR's output.
- Closed without implementation: R12 compounding, owner sweep, and handler balance/share narrowing. The final reasons live in [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md).
- Reopened 2026-08-31: **licensing**. MIT may not be the right license for BitChill, and `src/` declares MIT over GPL-2.0-or-later Uniswap imports today. Human decision, likely with counsel; free until cutover. See **Licensing — reopened** in [`IMPLEMENTATION_ORDER.md`](./IMPLEMENTATION_ORDER.md).
